Grocery Run — Roadmap
=====================

A single, prioritized backlog combining the earlier scattered notes with the
newer batch of ideas. Duplicates and closely-related items have been merged.
Tiers are ordered: cheap high-impact UX wins first, then the Ingredients-tab
cluster (which shares a refactor), then data-model changes, then the riskier
multi-device / integration work last. A Hygiene section after Tier 4 collects
the work that isn't a feature — tests, shared logic, and the database seam.


Tier 1 — small, high-value, low-risk
------------------------------------
1. [DONE — #11, #12] Recipe detail view. Tapping a recipe card in the Meals
   tab expands its full ingredients + notes, read-only, without having to hit
   Edit.
2. [DONE — #11] Remove a meal from "the list". After "Add to list" from the
   Meals tab, give an explicit way to take it back off. Also handle meals that
   reached the list via the Week plan, not just the Meals tab.
3. [DONE — #11] Suggested "unit" values when adding a new item.
4. [DONE — #11] Move the settings section off the bottom of the Ingredients
   page into a dedicated Settings tab (or a hamburger menu at the top). Do
   this before adding search/filter to the Ingredients tab so it isn't
   cluttered.
5. [DONE] Checked-off items: gray only the name, not the details. The whole
   List row no longer dims on check — only the item name gets the
   strikethrough + dim, so expanding a checked item keeps its "On the list
   for" breakdown at full opacity and legible.
6. [DONE] Week-plan meal picker. Each day/meal slot is now a tap-to-open
   button (showing the assigned meal, or "＋ Choose a meal") that opens a
   searchable modal mirroring the Meals tab — a search box plus recipes
   grouped "{type} meals" / "Other meals", each showing servings + tags, with
   the current pick checked and a "Remove meal from this slot" option. Replaces
   the native <select> that didn't fit the app's feel or scale with the list.


Tier 2 — Ingredients-tab cluster (shared refactor, then features)
-----------------------------------------------------------------
7. [DONE] Ingredients tab row treatment. Each row now collapses to just the
   ingredient name (with a subtle default-store · aisle hint), a gear, and an
   X. Tapping the gear or the name expands the row to edit the default store
   and per-store aisle numbers; Rename lives inside that expanded panel.
8. [DONE] Make the Ingredients tab filterable by store and A-Z, and add
   search. A search box (name match, with a clear ✕, mirroring the Meals tab)
   plus a store-filter dropdown ("All stores" / each store / Unassigned) that
   narrows to items with that default store. List stays A-Z sorted.
9. [DONE — #17] Ingredient rows show which recipes they're used in (inside
   the expanded gear panel, alongside whether it's also hand-added to the
   list) rather than as a separate info icon.
10. [DONE — #17] Add ingredients to the shopping list straight from the
    Ingredients tab. Each row gets a "+ List" button that becomes a
    ± quantity pill (with a tap-to-type-exact-amount) once it's on the list,
    defaulting to whatever unit recipes most commonly measure it in.
11. [DONE — #17] The List tab's "add an item" field now suggests matching
    known ingredients as you type (name + default store, keyboard-navigable),
    so you pick an existing item instead of creating a duplicate.
12. [DONE] Suggest units as you type when adding a new ingredient.
    Both halves are built — the conversion table first, the suggestions after.

    What shipped: a factor table in lib.js grouping units by dimension, and
    resolveAgainstBought doing the cupboard subtraction in base units so
    buying 1 lb offsets a recipe asking for 16 oz. That was the costly bug —
    the item stayed on the list looking unbought and you bought it twice.
    Amounts that can be added are added (1 lb + 8 oz reads 1.5 lb), and
    commonUnitFor now weighs by dimension so one "can" can't outrank an lb
    and an oz that between them say this is measured by weight.

    Two decisions worth keeping:
      - PROMOTION NEVER CROSSES MEASUREMENT SYSTEMS. Display picks the largest
        unit that keeps the number at or above 1, stepping down when it would
        fall below: 24 oz reads 1.5 lb, 1500 g reads 1.5 kg, a quarter pound
        reads 4 oz. It will promote to a unit nobody typed, because g/kg and
        oz/lb are one scale that everyone reads at a glance. It will NOT cross
        between them — answering "how much flour" in a system this household
        doesn't use is the surprise actually worth avoiding, and that is the
        one thing the metric/standard setting (item 31) would authorise.
        Two carve-outs, both learned by looking at the output:
          pt / qt / gal convert but are never promotion TARGETS. They're
          container sizes; a recipe wanting 2 cups of stock read "1 pt".
          Count units don't promote at all and show in the SMALLEST unit used:
          a dozen eggs plus two more is "14 ea", not "1.17 dozen".
      - US factors are defined from their exact ratios (1 lb = 16 oz, 1 cup =
        16 tbsp, 1 tbsp = 3 tsp) rather than via the metric base, so 16 oz and
        1 lb compare exactly instead of missing by a float hair and leaving
        0.0001 lb on the list.
    An amount with qty 0 ("salt, to taste") is kept at zero rather than
    dropped — an empty group is not the same as a covered one.

    [DONE] The suggestion half. Extended to every place a unit is typed — the
    recipe editor's ingredient row, the List tab's add box, and the amount
    editor on Ingredients.
    THE INGREDIENT'S OWN UNITS COME FIRST, and that is the only reason this is
    worth more than the flat list it replaces. Typing a unit for garlic offers
    `cloves` — twelve recipes use it — before `cup`, which is merely the
    household's commonest. The flat A-Z list was technically correct and went
    unused because the obvious answer was nine rows down.
    Ranked: this ingredient's units, then everything the household has typed
    ordered by HOW OFTEN, then COMMON_UNITS. Typed text narrows by prefix
    before substring, so "c" leads with `cup` rather than `oz can`.
    THE LAST TIEBREAK IS unitSuggestions' OWN ORDER, not alphabetical — a
    brand-new household has no usage to rank by, and alphabetical had it
    offering "bag, box, bunch" ahead of "cup" and "lb". Caught by the test for
    a fresh household, which is the case nobody using the app is ever in.
    AND IT IS NO LONGER A <datalist>. Those render unreliably — frequently as
    nothing at all on a phone, which reads as "this app has no suggestions"
    rather than a browser quirk. That is the same reason the ingredient-NAME
    field grew a custom list (item 11); the units kept the datalist for
    another year. One <SuggestInput> in ui.jsx now serves all three, taking
    its suggestions as a prop so ui.jsx still knows nothing about units.
    SUGGESTS, NEVER RESTRICTS. An exact match offers nothing — handing back
    the word somebody just finished typing is noise and leaves a dropdown over
    the field they are trying to leave — and a unit nobody has used is typed
    and kept, which the e2e asserts on what was PERSISTED rather than on the
    field, because a box that accepts text and drops it on save looks
    identical while you type.


Tier 3 — data-model / sorting
-----------------------------
13. [DONE] Pantry staples — things you own, kept off the list until you run
    out. `config[key].staple` marks one; `stapleNeeds[key]` says you're out.
    Two decisions that shaped everything after:
      - NO PARALLEL DATA STRUCTURE. A staple is an ordinary ingredient with a
        flag. "The staples list" is only ever a VIEW. Nothing to migrate,
        nothing to keep in sync.
      - THE SPLIT BY LIFETIME, which later features copied: the designation is
        a durable fact about the kitchen and lives in the catalog; being out is
        trip state and lives in the shopping state. Only "need" entries are
        stored, so an all-clear state tidies itself away.
    A staple carries forward if you didn't buy it, rather than needing
    re-entry — that is why it beats a weekly "what are we out of" prompt.

14. [DONE] Once an item is checked on the List, move it to the bottom of its
    group and keep following the A-Z / store-flow sort the user has selected.
15. [DONE] Recipes reference the shared ingredients list to avoid duplicates.
    The recipe editor's ingredient-name field now suggests matching known
    ingredients as you type (name + default-store hint, keyboard-navigable),
    reusing item 11's List-tab pattern, so you pick an existing item's exact
    spelling instead of forking a spelling variant into a separate ingredient
    and shopping-list line. Recipe ingredients still store { name, qty, unit };
    the suggestions steer them onto canonical norm(name) identities.


Tier 4 — multi-device / integrations (highest risk, do last)
------------------------------------------------------------
16. Formalize create/join household. Household codes already exist under the
    hood; this makes creating or joining one a first-class flow.
17. Delete / log out of households, and clean up orphaned data. Households
    created by devices we no longer use linger in the RTDB forever; a device
    with uncommitted changes on a separate household keeps it alive. Figure out
    whether deleting requires logging everyone out (or logging specific
    households out) and how to reclaim abandoned nodes.
18. Export to / link with a Google calendar, keyed off the household.

    Note: 16 and 17 are both patches on top of a catalog that can't be written
    at runtime. Moving the catalog into the database per account replaces them
    instead of extending them, and deletes the whole override layer with it.
    That's written up as a platform phase in FutureProjects.md ("Per-user
    catalog") — worth reading before starting either item.


Hygiene — not features, and not sequenced
------------------------------------------
None of these ship anything on their own. They're what keeps the app cheap to
change: a test layer, one place for shared logic, and a database seam narrow
enough to swap what sits behind it. Do them alongside whatever feature is next
rather than saving them for a cleanup that never gets scheduled.

19. [DONE] Tests on the pure layer. `npm test` runs node's built-in runner
    over lib.js — 100+ tests, no framework installed. CI runs lint, tests and
    build before deploying, in that order, so a failure stops the deploy rather
    than annotating one that already shipped.
    MUTATION-TEST ANYTHING LOAD-BEARING: break the rule deliberately, confirm
    the right tests fail, restore. A test that passes either way protects
    nothing, and several here originally did.

20. [DONE] Make a failed write visible. A rejected write used to be
    console.error and nothing else — invisible on a phone, where there is no
    console. Both writeHousehold and writeCatalog now report through a small
    pub-sub in sync.js (watchWriteErrors), which App.jsx subscribes to and
    reuses the SAME dot/label the offline state already draws with — no new
    visual language, just a truthful reason attached to the existing one.
    Offline is deliberately NOT this: the SDK queues writes in memory and
    flushes on reconnect without ever rejecting, so the signal only fires when
    the server actively refuses a write while connected (rules, quota, a
    malformed payload). Self-correcting, same shape as the update gate: it
    clears the moment any write succeeds again, no dismiss button needed.
    Done together with item 33 — same file, same theme, and fixing 33 first
    made 20 land on writes that no longer intermittently reordered anyway.

21. Keep the database seam narrow. Every database call lives in sync.js, and
    App.jsx is the only component that touches sync at all (SettingsTab
    imports syncEnabled, and nothing else — cleanCode moved to lib.js when
    parseInvite needed the same alphabet, which narrowed this further; the
    invite and member calls it needs arrive as props). That
    property, not any clever abstraction, is why RTDB could be swapped for
    Firestore in days rather than weeks. Defend it deliberately as usage
    grows and the right database stops being obvious:
      - no `firebase/*` import outside sync.js. Add an ESLint
        no-restricted-imports rule so it can't regress silently.
      - the seam should say WHAT CHANGED, not hand over everything.
        toggleChecked(key, on), not writeHousehold(entireState). RTDB
        implements that as a write at one path; Firestore as a document
        update. Neither is forced into the other's shape.
      - this also retires the update() footgun. Two update() calls in one
        handler discard the first, because both rebuild from the same stale
        snapshot — that is what broke the staple checkbox, and a comment on
        the function is not enforcement. Narrow writes remove the constraint
        instead of documenting it.
      - do NOT abstract queries or offline storage. Those genuinely differ
        between the two databases and a wrapper inherits the weaknesses of
        both. Keep filtering in JavaScript, and keep loadCache/saveCache as
        thin as they are — a Firestore migration deletes them outright rather
        than porting them.

    THE READ HALF IS STILL OPEN, AND IS DELIBERATELY DEFERRED. Narrow writes
    shipped; narrow listeners did not. One onValue at `state` delivers the
    WHOLE subtree however deep the write was, so every remote change
    re-normalizes the entire structure and re-renders everything.
    DO NOT DO THIS YET. Item 25's measurement killed the case for doing it now:
    the whole synced node is 954 BYTES, so the widest possible listener
    delivers under a kilobyte, and re-normalizing that plus a re-render is not
    worth a refactor that breaks four things (below).
    THE CATALOG MOVE WAS PREDICTED TO BE THE TRIGGER, AND IT WASN'T. This entry
    used to say the move would take the listened-to node from ~1 KB to ~30 KB
    and finally make the four costs worth paying — "do it immediately after
    that move, not before". The move shipped and that never happened, because
    the growth landed on its OWN node (households/{code}/catalog) rather than
    inside `state`. The listener at `state` still delivers about a kilobyte.
    The move made this case WEAKER, not stronger; item 37's "what's left"
    records the same conclusion from the other direction.
    THE REAL TRIGGER IS MEASURABLE, NOT ARCHITECTURAL: `state` itself getting
    big — per-person data, trip history, anything that stops it being one
    shared list — or re-render jank someone can actually feel on a phone.
    Re-measure with item 25's method before acting on it. Predicting this one
    by reasoning about the architecture is exactly what just went wrong.
    What it costs, so it isn't a surprise:
      - the atomic snapshot. Branches start arriving independently and out of
        order instead of as one coherent state.
      - pickState as written. It compares ONE updatedAt for the whole blob to
        decide local-vs-remote at launch; per-branch updates need per-branch
        stamps or a different staleness rule.
      - "is this a new household?", which today is just remote === null at the
        state node and drives the seeding path.
      - normalizeLocal splits into per-branch normalizers. That is where it
        should end up anyway, but it is work.
    Related measurement in item 25 — informative, not a gate.
    Background in FutureProjects.md ("Per-user catalog").

    UPDATE, once the catalog move shipped: the catalog went to its OWN node
    (households/{code}/catalog) with its OWN listener and its OWN updatedAt,
    not into the state node. So the ~30x growth this item was waiting for
    never lands on `state` — it lands next to it, already split. Ticking a
    checkbox still delivers under a kilobyte, and editing a recipe delivers
    the catalog without touching the list.
    That is one branch of the per-branch split this item describes, done for
    the branch that actually needed it, and it cost none of the four things
    above: each node keeps its atomic snapshot, keeps a single updatedAt that
    pickState reads unchanged, and keeps "remote === null means seed me".
    What remains open is splitting the STATE node itself (list vs plan vs
    stapleNeeds), and the case for that is still weak at 954 bytes. The next
    real trigger would be a third party in the household, not size.

    The adopt-vs-push rule is now shared. Both nodes route their listener
    through the same pickState: newer stamp wins, ties go to remote so two
    phones don't bounce a value between them, and no remote at all means
    seed. The catalog needed it for a specific reason — updateCatalog can't
    write before the listener has reported (writing early could replace a real
    catalog with a locally seeded copy), so an edit made offline sits on disk
    unwritten, and adopting on connect would silently discard it. A pristine
    seed carries updatedAt 0 precisely so it can never win that comparison;
    only an actual edit stamps a real time.

22. [DONE] List-building logic moved out of the tabs into lib.js. The tabs
    were 600-750 lines each, mixing data logic with rendering, and none of it
    was testable. Everything with a rule in it — aggregation, filtering,
    sectioning, unit conversion, ingredient identity — now lives in lib.js and
    is covered by item 19's suite.

23. [DONE] Ingredients have a stable id. The key used to BE the name, so
    renaming orphaned every reference — and two were already being lost in
    practice, because list.bought and stapleNeeds were added after the rename
    code was written and nobody went back.
      ingredients: { ing_7f3a2b: { name, store, aisles, staple } }
      recipe lines: { ingredientId, qty, unit }
    THE FILE STAYS NAME-KEYED. catalog.json is hand-edited and diffed in git;
    ids in it would mean inventing one and matching it across two sections just
    to add a recipe. Minted on the way in, resolved back to names on export.
    THE TRICK THAT KEPT IT SMALL: App resolves each line's id to a name when it
    assembles `data`, so every tab that reads i.name was untouched.
    Reads still accept a legacy norm(name) reference — expand phase. THE
    CONTRACT PHASE IS STILL TO DO: drop that fallback once both phones have run
    the migration and been used for a few days.
    The migration fires from the catalog LISTENER, never at startup. Converting
    offline would let a device mint ids, remap its state onto them, then adopt
    the other phone's catalog with DIFFERENT ids and orphan everything. Exactly
    one authority converts.

    TWO BUGS FOUND BY USING IT, NOT BY TESTS — both "code written when the key
    WAS the name":
      1. Four places read a key and treated it as a name. Two rendered
         "Ing_c45b0s82" where a name belonged; worse, the Ingredients add box
         and the recipe editor WROTE name-keyed entries into an id-keyed
         catalog, which would have reintroduced the old shape one entry at a
         time while looking fine. Everything that turns a typed name into an
         ingredient now goes through ensureIngredientId; everything that
         displays one uses ingredientNameFor.
      2. Renaming onto an existing name created a DUPLICATE. Impossible while
         the key was the name, and the old rename merged instead — a rule
         dropped along with the key-moving it needed. Renaming now merges,
         repoints recipes, sums a line that would otherwise duplicate, and asks
         first because a merge deletes an ingredient.
    THE LESSON: the migration was correct both times. What broke was ordinary
    EDITING afterwards. A migration test that only checks the conversion misses
    this entire class.

24. [DONE] Concurrently-edited collections are keyed by identity, not arrays.
    An array index is not a stable identity: two phones appending at once
    collide, and neither database can address one element without rewriting the
    whole list. diffPaths treats arrays as ATOMIC for that reason, so a keyed
    object is what makes a narrow write possible at all.
    Stores stay an array deliberately — order is meaningful (it drives
    store-flow grouping) and they're edited about never.
    Accepting the legacy shape on read is permanent, not transitional: Firebase
    hands arrays back as index-keyed objects regardless.

25. [DONE] Measure the wire. Two browser windows, DevTools on the WebSocket
    frames. The finding killed a planned optimisation: the whole synced node
    was 954 BYTES, not the ~30 KB assumed, because the catalog wasn't in it.
    A checkbox tick is ~130 bytes. Re-measure after any change that moves data
    into the synced node — arguing from an imagined payload size wasted a day.

26. [DONE] Week starts Sunday, not Monday — and make it a setting rather than a
    constant. DAYS is hardcoded in lib.js, so this is a small change to the
    array plus a place to choose. That place doesn't exist: the Settings tab is
    household sync and the publish flow, with no actual preferences section.
    Build the section first, then this is its first occupant. TIER 1.

27. [DONE] Sides on a dinner. A slot holds a main plus sides rather than one
    recipe. All of them feed the shopping list through aggregateItems, which
    already summed the same ingredient from several sources, so the list side
    needed nothing.
    Done AFTER the catalog move, deliberately — it changes the shape of a plan
    slot, and doing it first would have meant migrating that shape twice.
    Covered by tests/e2e/specs/sides.spec.mjs, added later — it shipped with
    no coverage at all, and the parts worth guarding turned out not to be the
    arithmetic but a side's LIFETIME: it belongs to the main it was chosen
    beside, dies with it, and is silenced with it by skipList. The 🥗 flag on
    a recipe only orders the picker; any recipe can be added as a side.

28. [DONE] Meals that don't feed the shopping list — leftovers, or a meal
    you're already stocked for. `skipList: true` on the slot, surfaced as
    "Already have the ingredients" on the slot's servings row.
    The label names the EFFECT, not one reason for it. Two list-facing walks
    honour it through one shared `slotFeedsList`; plannedMealCount deliberately
    does NOT, because a skipped meal is still a planned meal.
    Unticking DELETES the key rather than storing false, so a plan that never
    uses this keeps the shape it always had.

29. [DONE] Sticky controls, via a shared <StickyBar> in ui.jsx, on Meals,
    Ingredients and List. Invisible at rest; hairline and shadow only while
    actually stuck, read from a sentinel through IntersectionObserver rather
    than a scroll handler.
    WHAT TO PIN IS THE DESIGN, and it differs per tab: pin what you reach for
    WHILE scrolling, not everything that lives at the top. Pinning the whole
    List header came to three wrapped lines at 390px, so "Done shopping" and
    the meal count stayed below.
    A sticky ancestor with a z-index creates a stacking context — the Filter
    popover had to be checked with elementFromPoint, not by eye. The bar also
    cancels the page's 14px side padding, or rows show through at the edges.

30. Warn when another device is running an older build. Two phones on the same
    household diverged this week, and the only signal either gave was "Synced",
    which means the socket is connected and nothing about whose data won. Stamp
    each write with the build id already shown in Settings, and let a newer
    build say "another device is on an older version" when it sees an older
    stamp. An old build can't warn about itself — but the new one can warn
    about it, which is enough. TIER 4.

    Prerequisite, now done: the stamp has to name a commit that exists. On a
    pull request Actions checks out a temporary MERGE commit, so every preview
    was stamped with a SHA found nowhere in the branch. That cost a real
    debugging session during the catalog move — a phone was serving a stale
    preview and the only way to tell which build it had was matching its
    timestamp against gh-pages deploy times. CI now passes the pull request's
    head SHA through as BUILD_SHA and vite.config.js prefers it. Stamping
    writes with an id nobody can look up would have made this item worse than
    useless, so it had to be fixed first.

    Context, because it looked worse than it was: neither phone was old enough
    to do whole-state writes. The likely cause was #39's migration, which makes
    each device do ONE full set() the first time it writes after updating, to
    replace the old array shape. Both phones did that at about the same time
    and one overwrote the other. Known and accepted at merge time, spent once
    per device, and not recurring — but it took reading build stamps off two
    phones to establish that, which is what this item is for.

31. [DONE] Preferences: units (as-entered / metric / standard) and week start,
    in a Settings tab where every section is now tap-to-open.
    HOUSEHOLD, NOT DEVICE — they live on the catalog node. Two phones
    disagreeing about the week start would make the plan grid mean different
    things on each, which is worse than either answer.
    Display only; nothing is rewritten, so either can be flipped back freely.
    The units preference is what AUTHORISES crossing measurement systems, which
    item 12's conversion layer refuses to do unprompted. It also can't strand a
    total: counts and invented units have no metric form, so choosing one
    leaves them alone rather than dropping them.
    The week start ROTATES the presentation order and never renumbers: plan
    data is keyed by day name. Only WeekTab needs it — the aggregation walks
    DAYS to sum, and a sum doesn't care about order.
    Defaults are Sunday and standard. Note the trap that exposed: a default
    written as `x === "Sun" ? "Sun" : DEFAULT` silently makes the OTHER value
    unselectable once the default flips. Whitelist, don't compare.

32. [DONE] Update detection and the write gate.
    TWO MECHANISMS, KEPT SEPARATE, and that separation is the point:
      SOFT — the site is serving a different build. A modal offering Reload,
        dismissible per build. Right for almost every release.
      HARD — the household's catalog was written by a NEWER generation. Writes
        refused, reads untouched. A shopping list you can't open in a shop is
        worse than one you can't tick items off in.
    Both are modals, not banners: a banner at the top of a scrolling page is
    exactly what gets scrolled past. The gate keeps a banner TOO, so dismissing
    the modal doesn't leave editing silently dead.
    APP_DATA_VERSION (src/version.js) is the ordinal. A build SHA says two
    builds differ, never which is newer — that is why the gate needs its own
    number and can't reuse catalogVersion, which moves when a USER presses
    Export.
    The fix that made any of it work: main.jsx registered the service worker
    once and never called registration.update() again, so an app left in memory
    never learned a new build existed. It now checks on visibilitychange and on
    reconnect.
    The gate seeds from the CACHED catalog as well as the live listener —
    offline the listener never fires, and "the last catalog I saw was newer
    than me" is exactly when writing does damage. isBuildTooOld answers no to
    anything unparseable and fires only on strictly greater.
    STILL UNVERIFIED: the gate firing against a genuinely old build. Test with
    production in one browser and a PR preview in another, same household.

33. [DONE] Fixed the two require-atomic-updates races in sync.js, by
    sequencing rather than by racing to commit the newest baseline — the
    second option the item sketched, chosen because it removes the ambiguity
    by construction instead of needing a way to compare "newest".
    doWriteCatalog and doFlush (the actual network calls) now run through a
    tiny per-node queue (sequencer(): a chained `tail = tail.then(fn)`), so a
    second write's read of lastCatalog/lastWritten can never happen until the
    first write's has fully landed — no overlap, no ambiguity about which
    await resolves last.
    THE LINT RULE STILL FIRES, because require-atomic-updates is syntactic:
    it can't see that the sequencer makes the read-then-write-after-await
    pattern safe. Two targeted eslint-disable-next-line comments cover the
    exact lines, each explaining why — the first eslint-disable comments
    anywhere in this codebase, and a legitimate use of one: the rule's
    underlying concern is actually addressed, just not provably so from where
    it's looking.
    Verified by rapid-fire browser test — three recipes added back-to-back
    (writeCatalog) and three hand-added list items back-to-back
    (writeHousehold) — all landed with nothing lost, exercising exactly the
    overlap this item was about.
    Done together with item 20 — same file, same theme.

34. [DONE] Put the Firebase security rules in the repo, and published them.
    There was no rules file under version control — README.md carried an
    inline copy instead, which is not diffable and could quietly drift from
    whatever was actually live on the console.

    database.rules.json is now that source of truth, with firebase.json and
    .firebaserc alongside it so `firebase deploy --only database` targets the
    right project without extra flags. README.md's setup steps point at the
    file instead of embedding their own copy.

    WHAT IT DOES: deny listing every household at `/households` (nothing
    grants that read; only `$code` does, so only a specific known code
    resolves to anything); require `$code` to match cleanCode()'s own alphabet
    and be 8-40 characters (the README's prior inline rule only checked
    length, no character class); and validate that `updatedAt` — present on
    every write to state and catalog — is actually a number, catching a
    malformed payload rather than corrupting the shared blob.

    DELIBERATELY SHALLOW. No schema validation beyond those three scalars.
    normalizeLocal spreads `...d` specifically so an unrecognised field
    survives a build that doesn't know about it yet (tested in lib.test.js) —
    a strict `.validate` schema at the database layer would silently defeat
    that same guarantee from underneath the app, rejecting a legitimate
    future field the app itself is built to tolerate.

    PUBLISHED via the console's Rules tab (pasted from database.rules.json),
    not the CLI. Nothing in the build or CI touches the deployed rules — the
    committed file and the live database are two separate things kept in sync
    by hand, same as before this item except now there is a diffable copy to
    paste FROM. If the rules ever need to change again, that's still a manual
    republish, not something a merge triggers automatically.
    STILL NOT INDEPENDENTLY VERIFIED FROM THIS SIDE: nothing in this session
    can reach the Firebase console, so whether the deployed rules exactly
    match database.rules.json rests on the paste having gone cleanly. Worth
    a quick real check next time both phones are open — sync still working
    confirms it; item 20's write-error signal would surface it immediately if
    something in the paste was off.

    Real fix is auth — see FutureProjects.md and item 37.

35. [DONE] Add an error boundary. A render throw used to give a white screen,
    in a shop, with no way back. A class component in main.jsx now wraps
    <App/> and renders a "Something went wrong" fallback with a Reload
    button on any render throw. Reload is enough to recover because state is
    already on disk (loadCache) before anything renders — the boundary
    doesn't need to hold or restore it itself.
    Verified by mutation test: App() temporarily made to throw behind a
    query param, confirmed in a real browser (playwright-core against
    `vite preview`) that the fallback renders and Reload works, then
    confirmed the normal build shows no trace of the fallback. Change
    reverted before committing.

36. Split lib.js WHEN IT MAKES A CHANGE HARD, not on principle. It is 1,222
    lines and has become a grab-bag: units, ingredient identity, aggregation,
    storage, write-planning. Domain files (units.js, ingredients.js,
    aggregate.js) would be genuinely better.
    Deliberately not urgent. Splitting is churn with no behaviour change, and
    the file has not yet made anything hard to do. Revisit when it does.

39. [DONE] INGREDIENT LINES NEED A THIRD FIELD, and this is the root of several
    complaints at once. A line is { name, qty, unit } and there is nowhere to
    put "optional", "or ground turkey", "diced", "cooked separately", "2
    chopped 6 whole", or a can size — so BOTH a person typing and the recipe
    importer put it in `unit`, which is the one field that has to be exact
    for the arithmetic to work.
    IT IS ALREADY COSTING LIST LINES, measured across the 22 recipes:
      garlic            11 recipes "cloves", one "cloves (2 chopped, 6 whole)"
      crushed tomatoes  28 "oz can"  vs  1 "can (28 oz)"   — the same can
      salt              eleven "tsp", plus "tsp (can omit)" and "tsp (plus a pinch)"
      yellow onion      six with no unit, plus "cup, diced" and "small/medium"
      also black pepper, red pepper flakes, coconut milk, black beans,
      jasmine rice, ground chicken, heavy whipping cream
    Plan two of those together and the ingredient splits into two lines that
    the cupboard subtraction cannot offset against each other. Eleven
    ingredients are affected today.
    THE FIX IS A FIELD, NOT A PARSER. Add `note` (or `prep`) to the line,
    keep `unit` strictly a unit, and both the typing path and the import path
    stop corrupting aggregation. Do it EXPAND-then-CONTRACT: write the new
    field and read it while leaving whatever is in `unit` alone, ship, then
    migrate the existing bracketed units in a later pass. Retro-fitting the
    existing ones is a data change, not a code change, and can be done by
    hand once there is somewhere for the text to land.
    NOTE FOR THE MIGRATION: the older bracketed units are NOT damage. They
    are hand-written information a person deliberately recorded — can sizes,
    substitutions, prep. They should be moved, not deleted.

40. [DONE] Recipe import without AI. The paste-a-recipe importer (#86) did a poor
    job on three real recipes: modifiers went into `unit` ("pinch (to
    taste)", "(optional)", "small/medium"), "4 carrots cut into sticks"
    became `4 stick Carrots`, and it invented nine ingredients the catalog
    already had under another name — olive oil vs "extra virgin olive oil",
    salt vs "kosher salt", three separate cilantros.
    TWO SEPARABLE HALVES, and the second is the valuable one:
      - PARSING a line into qty / unit / name / note. Ordinary work once
        item 39 gives the note somewhere to go.
      - MATCHING the parsed name against ingredients that already exist.
        This is where the damage came from, and the app ALREADY has the
        machinery: the recipe editor suggests known ingredients as you type
        (item 15), and ingredientMatches/filterIngredients exist in lib.js.
        The importer bypassed all of it. An import that ASKS — "you wrote
        'extra virgin olive oil'; use the existing 'Olive oil'?" — is worth
        more than one that parses perfectly and forks the catalog.
    Prefer confirmation over cleverness: a wrong guess the user accepts
    silently is how a catalog acquires three cilantros.

41. [DONE] A "how it works" note before signing in, and sign-in easy to find.
    The first-run screen (item 37) offered three ways IN but never said what
    the app IS, so somebody who had never seen it was signing in to find out.
    THREE LINES, in the order the week actually happens: choose on Meals and
    Week plan, List builds itself from those recipes using the stores and
    aisles kept on Ingredients, both phones see the same List. Above the
    choices, and the test compares their POSITIONS on the page rather than
    their order in the source — an explanation under the buttons is not an
    explanation.
    EACH TAB IS NAMED IN BOLD, spelled exactly as the tab bar spells it, so
    the explanation doubles as the map: the four words in the list are four of
    the five things along the bottom of the screen (Settings is deliberately
    left out), and the first tap after signing in is informed rather than a
    poke. One <Tab> component holds the spelling, and the test asserts every
    bolded word IS a real tab label — a tab renamed without this line
    following it turns the map into a wrong one, and nothing else would
    notice.
    THE CARDS ARE REORDERED BY WHAT EACH ROUTE COSTS, not by how often it is
    used. Sign in is first. The invite card used to lead, which put the
    sentence "sign in below first, then come back and paste it" ABOVE the
    thing it was pointing at — you had to scroll past the answer to be told
    to go find it. It now says "above", and the test asserts that word.
    A GUEST LINK STILL SKIPS SIGN-IN ENTIRELY, and that has its own test:
    putting sign-in first must not quietly make an account a prerequisite for
    the one route deliberately designed without one.
    ASSERTED AS TEXT. There is no behaviour behind an explanation, so nothing
    else can catch it — the same reason the back-to-top button shipped
    rendering six literal characters with every behaviour test passing.
    THE SUITE NO LONGER KEYS ON THE COPY. "Which screen am I on" was matched
    against /Someone sent me a link/ in the harness AND in every onboarding
    spec, so rewording a heading hung the whole suite on a 15s timeout. The
    screen is now a landmark — <main aria-label="Getting started"> — which is
    what a screen reader wants anyway. Same lesson as sides.spec.mjs finding
    the quantity by DOM depth: identify a thing by what it IS.
    AND IT IS READABLE AGAIN, from "How it works" — the first section on the
    Settings tab, above Preferences, because somebody looking for "how does
    this work" opens Settings and starts at the top. The first-run screen is
    shown ONCE, before you have an account; without a second copy, the only
    explanation the app gives is one you cannot get back to.
    ONE COPY, IN src/help.js. The three lines were written twice for about a
    day and had already drifted. help.js is plain data — no React — so both
    screens read it, and a tab name is marked up {like this} rather than
    written as JSX. parseTabMarkup and searchHelp are the two pure halves in
    lib.js, because they are the two halves that can be WRONG.
    13 FAQs, each one a question somebody asked or a confusion the app caused:
    the two store dropdowns, why an ingredient shows "1.5 cup + 3 can", why
    the same thing is on the list twice, what Restore starter catalog costs.
    SEARCH MATCHES EVERY WORD, NOT ANY WORD, and that is the one behaviour
    here worth a test. With "any", a second word can only ever make the list
    LONGER — so typing more to narrow down does the opposite of what typing
    more means, which is the single most likely way for the box to feel
    broken. It searches the answer and the keywords too, because people search
    with the word on their mind ("supermarket") not the one in the text.
      THE FIRST VERSION OF THAT TEST DID NOT CATCH IT. It compared "store"
      against "store dropdown" — two words that happen to live in the SAME
      answer, so `some` and `every` give the same count and the mutation
      passed. It now uses "aisle" and "guest", which are in different answers.
      Pick the fixture that DISTINGUISHES the rule, not one that exercises it.
    NOT DONE, AND IT IS A SEPARATE ITEM (see 48): an invite is still a CODE
    you paste, not a URL you follow.

42. [DONE] THE SIGN-IN ERROR GIVES THE WRONG REMEDY. Signing in on a new phone
    showed "Signed in, but this account can't open household home-cx2ur9zg.
    Check the code above matches the other phone exactly." If the account is
    not a member, checking the code cannot help — the answer is an invite
    from someone already in. The message sends you to fix the one thing that
    is not broken. Cheap to fix and worth doing first.
    WORTH ESTABLISHING WHEN FIXING IT: whether the account eventually got in
    on its own. If it did, the message is merely misleading; if it never
    did, the resubscribe path (membershipTick) did not fire and that is a
    real bug behind it.

43. [HALF DONE, AND THE OTHER HALF DELIBERATELY NOT ATTEMPTED] Recipe
    instructions: number the steps and end each with a newline.
    DONE for anything pasted from now on: parseRecipeText numbers the steps,
    one per line, renumbered from 1, folding unnumbered continuation lines
    into the step above (#94).
    NOT DONE for the 22 recipes already stored, and this is the part worth
    reading before someone tries again. Their step boundaries are GONE, and
    mostly were never there: the notes were written as condensed prose, not
    pasted from a numbered source —
        Chicken Stir-Fry: "High heat, cook chicken first, vegetables second,
        sauce last."
    — and where paragraphs do exist they are not all steps. The first
    paragraph of Baked Chicken & Veggie Meatballs is a YIELD NOTE ("Yields
    about 24-30 1-inch meatballs"), so numbering paragraphs would label it
    "1.". Splitting on sentences instead would make "Halve the tomatoes." its
    own step. Only 2 of the 22 came from a paste with real numbered steps.
    Either rule invents structure that was not there and reads worse than the
    prose does. THE FIX IS RE-PASTING a recipe from its source next time you
    cook it, one at a time, which produces proper steps for free.

44. [DONE] Edit an ingredient from the List tab. Setting an aisle or a store
    currently means leaving the list, going to Ingredients, and searching
    for the item you were just looking at. The List row already expands;
    the editor could open there.
    Fits the "primary bits + details panel" pattern the Ingredients row
    already uses, and the List row is due that treatment anyway (see the
    UI-density note at the end of this file) — do them together.

45. [DONE] Keep tab navigation reachable while scrolling. On a long list, switching
    tabs means scrolling all the way back up. Either pin the tab bar or move
    it behind a menu button. Interacts with StickyBar and with the
    back-to-top control; check the stacking contexts by hit-testing rather
    than by eye, as those two both needed.

46. [DONE] Sort the catalog on export, so a diff is readable. Two catalog PRs in a
    row read as ~110 changed lines that were almost entirely key REORDERING,
    which hides the five entries that actually changed and makes review
    slow and unreliable. Sorting `config` keys (and recipes by name) on
    export makes every future catalog change diff to just the change.
    ONE-TIME COST: the first export after this re-sorts the whole file, so
    it wants its own commit with no content change in it, or the diff it is
    meant to fix is the one it produces.

47. [DONE] An over-long unit makes the row clickable. Reported from real use;
    reproduce before fixing. Likely the unit text wrapping and pushing the
    row's tap target, which item 39 will reduce but not eliminate.

48. [DONE] An invite link is not a link. `formatInvite` produces a bare code —
    `home-xxxxxxxx~token~g` — which is copied to the clipboard and PASTED
    into a field on the other phone. It is called a link everywhere in the
    UI, and the natural reading of "send them an invite link" is that tapping
    it opens the app already joined.
    Making it real means an actual URL carrying the code in the fragment
    (#join=...), read on first load and pre-filled into the join field. The
    FRAGMENT specifically, not a query string: it never reaches a server,
    which matters for something that grants access to a household.
    WORTH DOING BECAUSE OF WHAT IT REMOVES, not because it is tidier: the
    truncated-paste bug (item 37) exists only because a human is copying a
    50-character string between two phones by hand. A URL that is tapped
    cannot be half-copied.
    Check the PWA scope and the standalone-mode behaviour before starting —
    an installed app opening a link in the browser instead of itself would
    make this worse, not better.
    [DONE] It is a URL now, built with inviteUrl from the address the inviting
    phone is ALREADY ON rather than a configured domain — that is the one
    address known to work, and a constant would be a second thing to keep in
    step with wherever this is deployed. `base: "./"` is what makes that safe.
    THE FRAGMENT, NOT THE QUERY. Everything after `#` stays in the browser and
    never reaches the host or its logs. For a string that grants access to a
    household that is the difference between a link and a leak.
    READ ONCE AT STARTUP AND THE HASH CLEARED IMMEDIATELY, with replaceState
    so Back cannot re-arm it. Left in the address bar the invite is redeemed
    again on every reload, survives into a screenshot, and rides along with
    whatever the browser syncs between devices. That is the assertion worth
    keeping, not the convenience.
    parseJoinHash RETURNS THE STRING, not a parsed invite, and is handed to
    the same field a person pastes into. One definition of "valid invite", so
    a truncated link is refused exactly as a truncated paste is. A second
    parse would be a second place for that to be decided.
    TWO OTHER THINGS CAME OUT OF THE SAME REPORT:
      - THE CLIPBOARD WRITE COULD NEVER HAVE WORKED. It ran straight after
        `await createInvite(...)`, and Safari drops the user-gesture context
        across an await, so the write was refused and the "your browser
        blocked the clipboard" fallback came up EVERY time. The link is now
        shown with its own Copy button; that tap is a fresh gesture, which is
        the only kind the clipboard accepts. No test would have caught this —
        the sandbox browser allows the write.
      - THE SUCCESS MESSAGE WAS IN THE WRONG SECTION. copyText set `msg`,
        which renders inside "Export & recover" — three sections down and
        collapsed by default. So the button genuinely gave no feedback.
        Feedback now sits where the button is.
    AND THE PENDING-INVITE LIST NOW SAYS WHAT IT IS. An invite is written to
    the household the moment it is created — it has to be, or there is nothing
    to redeem — and the list sat under the member list reading as though
    somebody had just been added. "Invites waiting to be used — nobody has
    joined with these yet".
    NOT VERIFIED: redeeming needs the database, and the e2e build compiles
    sync out. Everything up to the redemption is covered; the redemption
    itself is not, and neither is the Settings join field, which lives inside
    the same syncEnabled branch.

    A LOOKUP KEY WAS CONSIDERED AND REJECTED — write it down, because the idea
    sounds safer than it is and will come back. Putting a short key in the URL
    and resolving it to {code, token} requires a phone that is NOT YET A
    MEMBER to read that mapping, which means a WORLD-READABLE index of every
    live invite. Today nothing about an invite is publicly readable at all:
    `invites` .read requires membership, and the joining phone never reads
    one — the RULES check it when the member record is written, which is why
    the token alone is enough. A short key is also enumerable, so the index
    could be harvested. Shorter URL, strictly worse. The fragment already
    never reaches a server, so there is nothing to buy.

    THE TOKEN IS THE ONE THING THAT WAS ACTUALLY WEAK, and it was fixed here.
    It base36-encoded each random byte and concatenated pieces of 1 or 2
    characters. Measured over 200k tokens: digits 1-6 at ~9.4% EACH against
    ~1.43% for most letters, 4.60 bits per character where uniform base36
    gives 5.17, and adjacent characters correlated because where one piece
    ends depends on its value. Probably still ~90 bits — and "probably" is the
    problem for a credential.
    Now 22 characters drawn uniformly by REJECTION SAMPLING: 256 is not a
    multiple of 36, so bytes >= 252 are discarded rather than folded with `%`,
    which would make the first four letters likelier. Measured after: every
    character 2.76-2.79% against a uniform 2.78%, 5.170 bits/char, 113.7 bits.
    It moved to lib.js in the process — it is pure logic, and in sync.js it
    could not be tested at all. The test asserts the DISTRIBUTION, because the
    old one did not look wrong; it looked like a random string.

50. A GUEST LINK IS REUSABLE FOR ITS FULL HOUR; A FULL INVITE IS NOT.
    Both are the same mechanism and only the `role` field differs, so this
    asymmetry is accidental rather than designed.
    joinWithInvite DELETES the token after redeeming it, and that deletion is
    what makes an invite single-use — the rules do not enforce it. But
    `invites/$token` .write requires `role != 'guest'`, so a guest who has
    just joined cannot delete the token they used. The remove() is refused,
    the catch swallows it, and the link stays live until `exp`.
    A full invite makes the joiner a non-guest member, whose delete succeeds.
    NOT OBVIOUSLY WRONG — two helpers sharing one guest link is a reasonable
    thing to want — but it was not decided, and the FAQ said "works once",
    which was false for half the cases. The wording is fixed; the BEHAVIOUR
    is still undecided.
    IF IT SHOULD BE SINGLE-USE, the rules have to do it, not the client: a
    guest cannot be given blanket delete on invites without also letting them
    revoke everyone else's. The narrow grant would be "delete a token whose
    id equals the `invite` field on your own member record".

51. UX EVALUATION — measured, not assessed. Driven in a real browser at 390px
    and 320px against the REAL catalog (22 recipes, 146 ingredients) with four
    dinners planned, not a fixture. Everything below is a number the page
    reported about itself; nothing here is a matter of taste.
    WHAT CAME BACK CLEAN, and is worth recording so nobody re-audits it:
      - NO horizontal page overflow on any tab, at either width. The item 47
        and 104 work held under the real catalog's longest names.
      - NO clipped text at 390px anywhere.
      - Settings is clean on every measure.

    RANKED BY WHAT IT COSTS:

    51a. THE MOST DESTRUCTIVE CONTROLS HAVE THE SMALLEST TARGETS. On
        Ingredients, "Remove <store>" is 11x15px — removing a store is the
        furthest-reaching action on that tab and it is a third the width of
        the 44px a thumb reliably hits. "Delete <recipe>" on Meals is 21x24.
        Both are destructive, both are the smallest things on their screen,
        and both sit next to something harmless. Fix the size, or put them
        behind the row's panel where the other editing lives.

    51b. FOUR TEXT COLOURS FAIL WCAG AA (4.5:1 for body text):
            "Done shopping"     4.09:1 @14px   (tomato on tomatoSoft)
            "Start a new plan"  4.09:1 @14px   (same pair)
            "4 sv"              4.15:1 @12px
            "Easy" pill         4.26:1 @11px   (gold on goldSoft)
        All are marginal, all are the soft-background pill treatments, and
        two of them are the primary action on their tab. A shop is often the
        worst lighting a phone gets used in. Darkening the ink rather than
        lightening the background keeps the treatment.

    51c. THE INGREDIENTS ROW SUBTITLE CLIPS AT 320px. "Grocery store · aisle
        8" needs 132px and is given 108. The store name is what identifies
        the row when scanning, and it is the half that gets cut.

    51d. THE WEEK PLAN IS THREE-QUARTERS EMPTY ROWS. Four meal types x seven
        days is 28 slots; a household that plans dinners uses 4-7 of them.
        Sunday alone fills a screen to show one planned meal. 2.5 screens to
        read four dinners. Collapsing empty slots, or showing only the meal
        types actually in use, would make the tab scan in one screen.

    51e. SCALE, for anyone sizing future work: at 390px the real catalog makes
        Ingredients 9,614px (12 screens) and Meals 5,449px (6.8). That is
        what back-to-top and the pinned tab bar exist for, and it is why
        search on those tabs is not optional.

    NOT MEASURED, and worth saying so: no VoiceOver pass, no one-handed reach
    testing, no timing of a real shop. The tap-target and contrast numbers are
    the mechanical floor, not the whole of usability.
    [All three were measured later — see item 58.]

    51a, 51b, 51c ALL [DONE] — item 57. 51d [DONE] — items 59 and 63, in two
    goes. 51e was information, not a fault.

MEASURED AND DELIBERATELY NOT PROBLEMS
    Recorded so nobody optimises them on instinct.
      - Bundle at 253 KB / 76 KB gzipped. Firebase is already dynamically
        imported in sync.js and code-split into its own chunks.
      - aggregateItems is O(recipes x ingredients) and runs on every render.
        Invisible at this scale. MEASURE BEFORE TOUCHING IT — the last time
        payload size was reasoned about rather than measured, the estimate was
        wrong by 30x (item 25).

37. [DONE] Multi-user: auth, and the household as a real tenant. Absorbed from the
    "platform phase" that used to live in FutureProjects.md, now that the
    expensive half of it has shipped.

    ALREADY DONE, and it was the hard part:
      - narrow, path-scoped writes (item 21's write half)
      - the catalog moved out of the repo and into the database per household
      - the override layer — configOverrides, recipeOverrides, localRecipes,
        extraStores, removedStores — deleted outright
      - the security rules committed and published (item 34) — the
        prerequisite this item itself named
    That override layer existed ONLY because catalog.json couldn't be written,
    and it is where almost every bug in this app lived: `false` as a "hidden"
    marker, compactCfg stamping "staple": false onto 114 ingredients,
    reconcileToCatalog guessing which local edits a new catalog now reflected.
    All of it is gone. Nothing below is blocked on it any more.

    [DONE — FIRST SLICE] Sign-in, purely additive. Google and email-link both
    land in sync.js, watched via watchAuthUser and completed on load via
    completePendingSignIn (covers both a returning Google flow and a clicked
    email link). Success writes a single users/{uid} record — email,
    displayName, updatedAt — and nothing else. Deliberately does NOT touch
    how a household is accessed: the code is still the whole story there,
    unchanged. The Settings "Account" section says as much, so signing in
    doesn't read as having done more than it has.
    Needed a users/{uid} rule added to database.rules.json — additive
    alongside the households rules, gated on `auth.uid == $uid`, the first
    rule in the file that checks a real identity rather than a shared secret.
    STILL A MANUAL STEP, same shape as item 34: Google and Email-link sign-in
    providers need enabling in the Firebase console (Authentication ->
    Sign-in method), the app's real domain needs adding under Authentication
    -> Settings -> Authorized domains (GitHub Pages, not a Firebase-hosted
    domain, so it isn't there by default), and the updated rules file needs
    re-pasting.

    VERIFIED ON A REAL PHONE, WHICH IS WHAT CAUGHT THE ACTUAL BUG. This
    sandbox has no path to Google's or Firebase's auth endpoints, so this was
    always going to need someone testing it for real — and it was worth it:
      - Email-link: worked cleanly once sent, and proved the whole rest of
        the pipeline correct in one shot — writeUserRecord, the users/{uid}
        rule, watchAuthUser, the "Signed in as ___" UI all fire off this one
        path too.
      - Google via signInWithRedirect DID NOT, and the failure was quiet in
        exactly the way item 20 exists to catch: the browser round trip
        through Google genuinely completed (approved, came back to the app)
        but getRedirectResult() resolved to nothing — no error, no user,
        nothing to catch — in BOTH Safari and Chrome. The pending-redirect
        state Firebase persists in IndexedDB across the navigation away and
        back wasn't surviving the round trip on this deployment. Never fully
        root-caused; not worth chasing once the fix below sidestepped the
        whole failure class.
      - FIX: signInWithGoogle now tries signInWithPopup first — the tab never
        navigates away, so there's no cross-navigation state to lose — and
        falls back to signInWithRedirect ONLY for auth/popup-blocked and
        auth/operation-not-supported-in-this-environment, not for
        auth/popup-closed-by-user (someone deciding not to sign in isn't a
        reason to silently retry a different way). Popup verified working on
        a real phone afterward.
      - HONEST GAP: that redirect fallback is UNVERIFIED and probably still
        broken, since the thing that broke it was never diagnosed, only
        avoided. It's harmless dead-weight to leave in — if it ever fires it
        likely fails the same silent way — but it is not a proven safety net
        and shouldn't be described as one. If a popup-blocked user ever
        reports being stuck, this is where to look first.
      - Also from this round of testing: completePendingSignIn used to only
        console.error a failed completion — invisible on a phone. It now
        returns { ok, code } and App.jsx surfaces a real failure as a banner
        in the Account section, same "make the failure visible" instinct as
        item 20. And the send/sign-in feedback messages were upgraded from
        identical small gray text for success AND failure (which is what
        made the redirect failure read as "nothing happened" in the first
        place) to colored, weighted text that includes Firebase's actual
        error code — so the NEXT silent-looking failure won't be silent.

    [DONE — EXPAND PHASE] Household membership, re-parenting step 1 of 3.
    Re-parenting the household under a real account is being done as EXPAND
    then VERIFY then CONTRACT (same discipline as the catalog move), because
    it's an access change two people's phones both have to survive. This is
    only the EXPAND phase.
      - Whenever a signed-in account is looking at a household it already has
        the code for, App.jsx now calls recordHouseholdMembership(code, user)
        (src/sync.js), which writes households/{code}/members/{uid}: {email,
        displayName, updatedAt}. Re-fires on sign-in/sign-out and on switching
        households — the two ways the pairing can change.
      - Grants NOTHING new. Access is still the code alone, exactly as
        before — households/$code keeps its blanket .read/.write. The new
        database.rules.json block is a .validate, not a .write, because
        .write/.read only ever ADD permission as the rule tree goes deeper;
        a broad ancestor grant can't be narrowed by a rule underneath it.
        .validate is the mechanism that actually constrains WHAT gets
        written there regardless: auth.uid == $uid, so a member record can
        only ever be written by the account it claims to represent, never
        forged for someone else.
      - Nothing reads this yet. It exists purely to start accumulating, for
        real, the record that VERIFY and CONTRACT need before requiring
        membership can be safe. Doing that on day one of using the app would
        mean requiring membership before anyone had one.

    [DONE — VERIFY] Confirmed against the live database, not by reasoning.
    The real household (home-cx2ur9zg) carries a members record for each of
    the two accounts, and each holds exactly the three fields
    recordHouseholdMembership writes. Timestamps are the useful part:
      - 2026-08-06 23:57:49 UTC, one account. That is 28 minutes after the
        build carrying this code went live, which is what proves the write
        came from deployed code rather than something left over from
        testing.
      - 2026-08-07 18:38:44 and 18:38:54 UTC, both accounts.
    THE SECOND ACCOUNT DID NOT DISTURB THE FIRST, which was the actual risk
    worth checking. The write is set() on members/{uid}, not on members, so
    a second account adds a sibling instead of replacing the node. Both uids
    coexist, and the first one's record survived intact.

    SIGNING IN DOES NOT ADD AN ACCOUNT TO EVERY HOUSEHOLD. Checked
    deliberately, because two separate households ended up holding both
    accounts and that looks exactly like a fan-out bug. It isn't one, and
    four independent things prevent it:
      - there is ONE write to any members path in the whole codebase.
      - it has ONE caller, which passes the single currently-selected
        `code` — a scalar, never a collection.
      - no code anywhere references `households` without a `${code}`
        segment, so nothing can enumerate them, which a fan-out needs.
      - /households itself has no `.read`, so the database refuses to list
        households even if a client tried.
    Empirically: six of the eight households have no members node at all.
    A fan-out would have written to all eight. What actually happened is
    that one browser session moved between two household codes and two
    accounts inside 76 seconds, and each write recorded the (code, account)
    pair that was active at that moment — which is the intended behaviour.
    The extra households are test litter, mostly from browser-driven
    verification: firebase-config.js commits a real databaseURL and
    syncEnabled is just Boolean(databaseURL), so ANY browser loading ANY
    build talks to the production database and mints a code on first run.
    Worth deleting them from the console before CONTRACT, since after it
    the two that hold both accounts would otherwise read as real
    households.

    [BUILT, NOT YET DEPLOYED] CONTRACT. database.rules.json now requires
    BOTH a real identity and a membership record to read or write a
    household. There is deliberately no unauthenticated fallback: a device
    that isn't signed in reads and writes nothing. Done as one step rather
    than a transition window with an `auth == null` branch, because the
    second phone is being signed in as part of delivering this — a window
    nobody needs is two pastes and two rounds of app changes for nothing.
    members/$uid needed its own ".write" (it had only ".validate"). The
    $code-level ".write" now requires membership, so the account that needs
    that write is precisely the one that isn't a member yet; without a
    deeper grant, joining is impossible by construction — the write that
    makes you a member would require already being one. That deeper grant is
    the one door into a household, and it opens only at the caller's own uid.
    Access is now identified and enumerable. Self-service joining is GONE
    as of the invite work below, so removal actually sticks.

    REQUIRING AUTH FORCED APP WORK, and it is the more interesting half.
    Once a signed-out phone is refused everything, the app had three ways to
    lie about it:
      - THE GREEN DOT. syncStatus comes from watchConnection, which reads
        .info/connected — a client-side path NO security rule gates. It
        reports "synced" perfectly happily while every read is refused, so
        a signed-out phone would have shown "Synced" over a dead listener.
        That is precisely the "looks fine, syncs nothing" failure this
        codebase keeps paying for. The precedence now lives in
        syncIndicator() in lib.js: sign-in state and authorization both
        outrank connection state, and connection is LAST because it's the
        one the database can contradict. Each case is the CAUSE of the ones
        under it — "Sign in to sync" says what to do, where "No access",
        equally true at that moment, leaves you guessing.
      - A SILENT DENIAL. onValue had no cancel callback, so permission_denied
        did nothing at all — indistinguishable from "connected, nothing has
        changed yet". subscribeHousehold/subscribeCatalog/subscribeMembers
        now take onError. Two properties matter: Firebase REMOVES a listener
        it cancels, so a denial is terminal and only a resubscribe recovers;
        and cb is never called on a denial, so a refused read can't arrive
        as `null` — which would read as "no household here, seed it" and
        turn a permissions problem into a write.
      - THE COPY. Settings still said sign-in was "optional, and doesn't
        change anything yet". It is now the thing that grants access.
    IT IS IN lib.js, NOT App.jsx, FOR A SPECIFIC REASON: the case that
    matters most — a socket genuinely CONNECTED while reads are refused —
    cannot be reproduced in a browser here, because this sandbox can't reach
    Firebase at all and the socket just reads offline. A pure function takes
    no network. Six tests cover it, and both mutations were confirmed:
    moving the "synced" check above the authorization checks fails exactly
    the three tests about precedence, and dropping the authReady guard fails
    exactly the one about not claiming "signed out" before auth has
    answered.
    authReady exists because `user === null` means two opposite things —
    "don't know yet" before Firebase answers, "nobody" after. Auth restores
    asynchronously, so treating the initial null as signed-out would flash
    "Sign in to sync" on every launch of a signed-in phone.

    THE RESUBSCRIBE RACE, found while reasoning about the rules and fixed
    with them: the household/catalog subscribe effect only re-ran on `code`,
    never on `user`. A device signing in AFTER that effect's first run — the
    common case, since auth restores after mount — would have its listener
    denied and never retried. It now depends on a `membershipTick` bumped
    once recordHouseholdMembership's write actually LANDS, not the instant
    sign-in state changes, which would still race the write and just get
    denied again.
    Work done while signed out is not lost: updateCatalog and setLocal still
    write to localStorage, and pickState pushes whichever copy is newer once
    the listener is allowed again. So signing in flushes what the phone did
    while it couldn't reach the database.

    A BUG FOUND BY READING THE SCREEN, not by any test: "Invite another
    phone" and "Guest link" did the same thing. The wrapper App passed down
    was `(ttl) => createInvite(code, user, ttl)` — one parameter — so the
    second positional `role` was silently swallowed and every invite was a
    FULL one. The guest button still labelled its link "~g", so the link
    claimed guest while the stored invite said member, and the rules
    correctly refuse a redemption where those disagree. The result was a
    guest link that could never be redeemed, produced by a button that
    looked like it worked.
    NO TEST WOULD HAVE CAUGHT IT, and it is worth being honest about why:
    creating an invite needs a database, so the e2e build (sync compiled
    out) cannot reach the call at all, and the rules tests exercise the
    RULES, which were right — they refused exactly as designed. The failure
    lived in the four inches of prop wiring between them.
    FIXED STRUCTURALLY RATHER THAN BY A TEST. createInvite takes an options
    object, because one argument cannot be half-forwarded; and it returns
    the role it actually wrote, with the link built from THAT, so a caller
    can no longer describe an invite the database didn't store. Both changes
    make the bug unexpressible instead of merely detected.

    ALSO SHIPPED: the member list (was listed below as the natural follow-on
    to CONTRACT, and it earns its place now that access can actually fail).
    Settings shows households/{code}/members, read via a prop from App.jsx
    rather than importing sync into SettingsTab — item 21's seam holds.
    Reads the denormalized email/displayName exactly as intended, never
    users/{uid}, which the rules keep private per account.

    VERIFIED AGAINST THE LIVE DATABASE, on the #81 preview, which is what
    this could never be from a sandbox. All four flows reported working:
    signing out and back in mid-session (the resubscribe path), a full
    invite end to end, a guest link, and removal taking access away.
    WHY THE PREVIEW COUNTED: a PR preview is an ordinary build — only the
    e2e build strips sync — so it talks to the real household and the real
    rules. That is the same reason it left real residue: an anonymous guest
    persists as a members/{uid} record until removed, unused invites live
    until they expire, and a fresh browser that chose "start my own list"
    minted another home-* household.
    Previously confirmed in a sandbox browser, and still true: a signed-out
    device says "Sign in to sync" rather than "Synced", the stale "doesn't
    change anything yet" copy is gone, and the member list renders its
    signed-out state.
    THE TWO WORTH RE-CHECKING IF ANYTHING EVER SEEMS OFF, because both fail
    in ways that look like nothing happening:
      - sign-out/sign-in mid-session. A denied listener is REMOVED by
        Firebase, not retried, so a broken resubscribe shows as a list that
        simply stops updating — no error, no dot change. Watching one phone
        change and the other follow is the only real check.
      - removal. It looks like it worked the moment the row disappears. The
        actual property is that the removed account can't get back in with
        the household CODE, which needs trying.

    [BUILT, NOT YET DEPLOYED] INVITES — the step that makes removing
    somebody mean something. Before it, membership was self-service: any
    signed-in account holding the code wrote its own record, so taking a
    member out was decoration — they knew the code, so they rejoined.
    An account now becomes a member exactly three ways, all concentrated in
    the members/$uid ".write":
      1. it already has a record (refreshing it, or deleting it to leave)
      2. the household has NO members — first claim, which a brand new code
         needs and which no occupied household can be turned into
      3. it presents an unexpired invite token created by an existing member
    Plus one exception that only ever TAKES access away: a member may DELETE
    another member's record, never create or edit one.
    An invite is households/{code}/invites/{token} = { by, byEmail,
    createdAt, exp }, created only by a member, expiring in an hour.
    THE REDEEMER DELETES IT AS A SECOND WRITE, not atomically with joining.
    At the moment of redeeming they aren't a member yet, and letting a
    non-member delete invites would let anyone holding the code burn every
    outstanding one — a denial of service on joining. Doing it immediately
    after works because by then they ARE a member; if that write is lost the
    invite just expires.
    THE CODE STILL SWITCHES HOUSEHOLDS for an account that is ALREADY a
    member. That is the recovery path — a reinstalled phone mints a new
    device code and has to get back to the real household — and it is safe
    precisely because it grants nothing: the rules check membership, not the
    code. One field in Settings takes either, since a user handed a string
    shouldn't have to classify it.

    RULES ARE NOW TESTED, which is the part that makes any of the above
    trustworthy. tests/rules/ runs the REAL Firebase database emulator
    against the REAL database.rules.json — 24 cases covering the stranger
    who knows the code, forged records, expired and revoked and reused
    invites, removal sticking, first claim, and the pre-existing guarantees
    (no enumerating /households, updatedAt must be a number, users/{uid}
    private). `npm run test:rules`, wired into CI before the build, skipping
    rather than failing where there's no JVM or emulator jar — BUT ONLY
    LOCALLY. In CI a skipped suite is indistinguishable from a passing one:
    the step goes green having asserted nothing, which is worse than not
    running it at all, and is the exact failure this suite exists to prevent
    everywhere else. So `process.env.CI && !ready` throws instead.
    FOUND BY TRYING TO CONFIRM IT, not by reasoning: the first real workflow
    run went green, and the log tail could not show whether the step had RUN
    or merely skipped. The only evidence was circumstantial — a 32 MB
    emulator jar in the post-job cache — which is not a good enough basis
    for the one thing standing between a rules mistake and a phone locked
    out of the shopping list.
    Mutation-tested both ways: restoring self-service join fails exactly 7
    cases including "a stranger cannot make herself a member" and "removal
    sticks"; opening invite creation to non-members fails exactly 2.
    THE CLI DOESN'T WORK HERE and the harness deliberately doesn't use it:
    `firebase emulators:start` routes its own rules upload through this
    sandbox's HTTP proxy even for 127.0.0.1, which answers "denied by ..."
    and fails before any test runs. The harness drives the emulator jar and
    its REST API directly, minting unsigned "alg":"none" JWTs the way
    @firebase/rules-unit-testing does.
    THIS DOES NOT PROVE THE CONSOLE MATCHES THE FILE. That is still a manual
    paste. It proves the thing being pasted is right before it goes, which
    is the half that was previously pure reasoning.

    A BUG THE BROWSER FOUND AND THE UNIT TESTS DIDN'T: a truncated invite
    ("home-cx2ur9zg~short") parses as no invite, and the join field then
    fell through to treating it as a household CODE — cleanCode strips the
    `~`, leaving "home-cx2ur9zgshort", a different household, which the app
    cheerfully offered to switch to. Switching replaces this phone's list,
    so silently resolving a bad paste to the wrong household is the most
    expensive possible reading of it. The decision moved into lib.js as
    classifyJoinInput (code / invite / broken / short) with tests: a `~`
    present at all means an invite was intended, and if it doesn't parse
    that is broken, never a code. parseInvite alone had passing tests the
    whole time — the hole was in what the component did with a null.

    [BUILT, NOT YET DEPLOYED] THE GUEST ROLE. Someone you hand a link to so
    they can do the shop: reads everything, writes only the List tab.
    Reading the whole household is deliberate — a list of names with no
    recipes behind it isn't shoppable, you can't tell what a thing is for.
    members/{uid}.role is "guest", or absent/"member" for full. ABSENT MEANS
    FULL, which is what makes this backward compatible: the records written
    during EXPAND have no role and belong to the two people who own the
    household.
    HOW IT'S ENFORCED, and the shape is worth remembering: households/$code
    ".write" became full-members-only, and the guest's permissions are
    re-granted BENEATH it at state/list, state/stapleNeeds and
    state/updatedAt. A deeper ".write" adds permission where a shallower one
    denied, so denying at $code and re-granting narrowly is what produces
    "everything except these paths is read-only". updatedAt has to be in the
    list because every narrow write carries it; without it a guest's write
    would be half-refused and the other phone would never see the change.
    NOT re-granted, deliberately: plan and planStage. Planning the week and
    starting or ending a trip belong to the household.

    THE ESCALATION THAT ALMOST SHIPPED, caught by a test. Every member
    rewrites its own record on each load, so a guest could simply write
    role: "member" and promote itself. The fix is role immutability — but
    the FIRST attempt put that check in members/$uid ".write", where it
    silently did nothing for the people it mattered least about and
    everything for the people it mattered most about... backwards: the
    $code-level ".write" grants a FULL member everything beneath it, and a
    deeper ".write" can only ADD permission, never take it back. So the
    check constrained guests and strangers but did not apply to full members
    at all. It lives in ".validate" now, the only rule kind that always
    runs. A second test caught the corollary: ".validate" doesn't run on
    deletes, which is what lets a guest still leave.
    A knock-on: the membership refresh had to move from set() to update(),
    because a set() that drops `role` reads as CHANGING it and is refused.

    THE ROLE RIDES IN THE INVITE STRING ("code~token~g"), because the
    account redeeming an invite cannot read it — invites are members-only
    and a joiner isn't one yet. That is not where it's enforced: the rules
    require the record written to match the STORED invite, so editing the
    "~g" off a link is refused rather than upgraded. Tested both ways round,
    including the quieter direction — omitting the role entirely, which
    would otherwise default to full.
    parseInvite got stricter with it: a third segment that isn't exactly "g"
    is now refused outright rather than folded into the token, so a mangled
    guest link can't resolve to a full membership.

    THE APP MIRRORS THE RULES RATHER THAN LISTING TABS. guestBlockedFields
    (lib.js) says "everything except list, stapleNeeds and updatedAt is off
    limits", checked against the RESULT of an edit in App's update(). That
    catches every route into a forbidden field, including ones that don't
    exist yet — a new top-level field is denied to a guest by default,
    exactly as the rules do. Gating per tab would have meant auditing four
    tabs and re-auditing on every feature. updateCatalog is stopped once,
    centrally, since the whole catalog is full-members-only.
    A refused edit gets a sentence naming what's off limits, not the generic
    "Sync error" a rejected write produces.
    [DONE] THE BUTTONS A GUEST CAN'T USE ARE NOW HIDDEN, not disabled — a
    greyed-out "Clear week" asks how to un-grey it, and the answer isn't
    something a guest can do. What goes: creating and editing recipes,
    everything on the week plan, the stores card, adding an ingredient, and
    a row's store/aisle/staple/rename/remove. What STAYS, because it is a
    list write or a read: adding a meal to the list, the whole List tab, the
    "used in / on the list" footer, and every tab remains visible — a guest
    needs the recipes and the week to know what the list is for.
    Settings' Preferences and Restore go too; saving a backup is a read and
    stays.
    TESTABLE AT ALL ONLY VIA A SEAM: guest-ness comes from a members/{uid}
    record, which a sync-stripped build can never hold, so the guest UI would
    have been unreachable in the e2e suite. GUEST_PREVIEW_KEY is read ONLY
    when syncEnabled is false — a production build takes the real branch and
    never looks at it, and the rules refuse the writes regardless of what any
    client believes.
    EVERY CASE ASSERTS BOTH DIRECTIONS, because hiding too much is the easier
    mistake and the harder one to spot: the thing a guest must not see is
    absent AND a full member still sees it. Mutation-tested both ways —
    showing the editors to a guest fails the guest case, hiding them from
    everyone fails "a full member still sees everything".
    A TEST BUG WORTH REMEMBERING: Playwright's `has-text` is a
    case-insensitive SUBSTRING match, so `button:has-text("Edit")` matched a
    recipe called "M-edit-erranean Baked Crispy Chicken" and reported a guest
    being offered two Edit buttons that did not exist. Use `text-is` for an
    exact button label. The member control is what made this legible as a
    selector fault rather than an app fault.

    Rules coverage went 24 -> 38 cases, all against the real emulator.
    Mutation-tested: removing role immutability fails exactly the two
    promotion/demotion cases; giving guests the full $code write fails seven.

    [BUILT, NOT YET DEPLOYED] FIRST RUN, AND GUEST LINKS THAT DON'T NEED AN
    ACCOUNT. Two problems, one cause: nothing on screen ever told a new
    browser what to do. It minted its own household code and landed on a
    working list seeded from catalog.json — which LOOKS fine, and that is
    the trap. Someone handed an invite ended up in a private household of
    their own, staring at a list that wasn't the shared one, with nothing
    suggesting anything was wrong. The app was answering a question they
    hadn't asked.
      - src/Onboarding.jsx: three ways in, the one most people arrive with
        (a link somebody sent) first, then signing in, then "start my own
        list". Shown ONLY to a browser with no household data and nobody
        signed in, so neither of the two existing installs ever meets it.
        Gated on authReady for the same reason the sync indicator is: `user`
        is null before Firebase answers, so without it a signed-in phone
        would flash the first-run screen at every launch.
      - ANONYMOUS AUTH for guest links. A guest link now signs the browser in
        anonymously and asks for a name, so being handed a link is the whole
        flow — no account to make. Chosen over requiring a real sign-in
        because a guest is inherently a one-off: the friction of an account
        is most of the cost of asking someone to do the shop.
        THE COST, stated plainly: the identity lives in that browser's
        storage and dies with it, so clearing site data means asking for a
        new link. The typed name is what keeps the member list answerable —
        without it a guest shows as a bare anonymous id, which would undo
        most of what CONTRACT was for.
        NEEDS THE ANONYMOUS PROVIDER ENABLED in the console, same manual
        step as Google and email-link.
      - THE RULES CAP IT HARD, and this is the part worth keeping:
        auth.token.firebase.sign_in_provider != 'anonymous' now guards case 2
        (first claim) and full-invite redemption. An anonymous identity may
        be a guest and nothing else — it can't own a household and can't hold
        one as an equal, because both should mean an account that can be
        recovered and named. Mutation-tested: dropping the guard fails
        exactly the two cases that assert it.
      - A KNOCK-ON WORTH REMEMBERING: recordHouseholdMembership refreshes the
        member record on every load, and it used to send email/displayName as
        `|| null`. For an anonymous guest both are null on the auth user —
        the name exists ONLY in the member record — so the refresh would have
        wiped the typed name on the next load. It now sends only the fields
        it actually has.

    SETTINGS SAYS "HOUSEHOLD" NOW, not "Phone-to-phone sync". That name was
    the old model — a shared code WAS the whole story, and hasn't been since
    membership landed. Members and invites were buried underneath it, which
    is exactly backwards: they are what the section is for now, so they come
    first and the code drops to the bottom as the detail it has become.
    Opens by default, because it is the thing you go to Settings for.

    THE E2E HARNESS NEEDED A CONCEPT IT DIDN'T HAVE. openApp seeds a device
    code but no state, so with the first-run screen live every existing spec
    suddenly landed on it and timed out looking for a tab bar. It now seeds
    the onboarded flag by default (these specs test the APP), with
    onboarded:false to opt out. The WAIT TARGET mirrors the app's own rule —
    cached state counts as onboarded there, so seeding `state` means the app
    screen even with the flag withheld. Deriving it any other way makes the
    harness disagree with the thing it is testing, which is how a harness
    starts lying.

    WHAT'S LEFT
      - ENABLE THE ANONYMOUS PROVIDER in the Firebase console (Authentication
        -> Sign-in method), or guest links fail at the sign-in step with a
        message rather than working.
      - DELETE the throwaway home-* households in the console FIRST. After
        CONTRACT the two holding both accounts would read as real ones.
      - SIGN BOTH PHONES IN **BEFORE** PASTING, and check each shows up in
        Settings' member list. Both accounts already have member records
        from the EXPAND phase, so nobody is locked out by the paste — but a
        phone that is signed OUT when the rules land stops syncing on the
        spot. Nothing is lost when that happens (edits keep saving locally
        and push once it's allowed again), it just looks broken.
      - PASTE database.rules.json (same manual step as item 34).
      - VERIFY ON BOTH PHONES afterward. Three things, none of which this
        sandbox could check because it has no path to Firebase:
          * sign out and back in mid-session — the list must resume
            updating rather than going quietly stale (the resubscribe path).
          * invite the other account end to end, from a real Settings
            screen. Two browsers and two emails is enough — an invite is
            accepted for an ACCOUNT, not a device, so two profiles signed
            in as different accounts exercises the real path.
          * hand a GUEST link to the second account and confirm the shape
            of it: the list works, and recipes / ingredients / the week
            plan refuse with the guest banner rather than failing silently.
          * remove an account and confirm the code alone does NOT let it
            back in. That is the whole point, and the only proof that
            matters is the live database.
      - Narrow listeners: item 21's read half. Still deliberately deferred,
        and the catalog move made the case WEAKER rather than stronger, since
        the growth landed on its own node instead of on `state`.
      - Hide the controls a guest can't use (Meals, Week, Ingredients).
        They explain themselves when tapped today, which is honest but not
        as good as not offering them.
      - Billing, a landing page. Ordinary product work, no foundation
        questions left in them.
        On roles: guest/member is now the whole ladder, and every FULL
        member is equal — anyone can remove anyone, including the account
        that created the household. Fine for two people who live together;
        an owner who can't be removed is the next rung if a third full
        member is ever in one.

    Firestore is not a step here. It becomes worth adopting when one of its
    advantages actually binds — most likely offline persistence, or history
    outgrowing what a client-side slice can page through. Item 21 covers what
    keeps that door open.

38. A nicer URL than stuart-belcke.github.io/grocery-run/. Came up while
    walking through Authorized domains for item 37 — the current URL works
    fine but looks like a personal project tucked under an account, not
    something you'd hand to someone else.
    DECIDED: not worth a purchased custom domain right now. A domain is
    ~$10-15/year for something purely cosmetic today; only actually buy one
    if some future distribution requirement forces it — e.g. listing this
    somewhere (an app store, a share link that has to look legitimate) that
    itself demands a real registered domain. Revisit if that happens, not
    before.
    THE FREE OPTION, if/when the current URL is worth improving without
    paying: a GitHub Organization (not a second personal account — no extra
    login to manage) named grocery-run gets grocery-run.github.io for free,
    the same way any account's own `<name>.github.io`-named repo does.
    What that would actually take, so it isn't underestimated as a five-
    minute change:
      - create the free org, transfer this repo into it, rename the repo to
        grocery-run.github.io (the exact name is what unlocks the root URL
        instead of a subpath).
      - .github/workflows/deploy.yml builds URLs assuming a PROJECT site
        (`<owner>.github.io/<repo>/`) today — both the live-site verification
        curl and the PR-preview mechanism need checking against a root-served
        user/org site instead.
      - add grocery-run.github.io to Firebase Auth's Authorized domains
        (additive — stuart-belcke.github.io doesn't need removing, in case
        anything still points at the old URL during the switch).
      - the app itself needs NO changes — vite.config.js's `base: "./"` was
        already deliberately built to work at any URL depth, checked when
        this question came up.
    Not scheduled. Written down so the org-vs-domain tradeoff doesn't need
    re-deriving next time the URL feels worth fixing.

Refactor notes
--------------
[DONE] The catalog move was done as EXPAND then CONTRACT, and it's the pattern
  to reuse for any future shape change that two devices have to survive.
    expand   — the new path (households/{code}/catalog) is added and read, and
               the old fields are left in place, written by nobody and read by
               nobody. Rolling back is reading them again, not restoring a
               backup. Shipped as one release, then actually used on two phones
               for a day.
    contract — the old fields come out of the code once the new path is proved.
  Two things made the contract step cheap. normalizeLocal spreads `...d`, so
  retiring a field carries it through instead of pruning it — there is no
  migration write to get wrong, and a device that never saw the new build keeps
  its copy. And every reference was in lib.js by then, because the read half had
  already moved in the expand release.
  The traps, both caught by tests rather than by reading: validLocal gated on
  localRecipes existing, so removing it from emptyLocal would have made every
  new device's state read as invalid; and needsKeyMigration checked localRecipes
  for a legacy array shape, which after the contract would force a needless full
  set() to repair a disagreement that no longer exists.

[DONE] App.jsx was split so each feature edits a small, reviewable surface:
  - theme.js + ui.jsx: UI primitives (colors, fonts, Btn, Seg, Stripe, inputStyle)
  - lib.js: pure helpers (constants, catalog/config normalization, storage,
    model normalization, list aggregation)
  - src/tabs/*.jsx: per-tab components (List, Meals, Week, Pantry, Settings)
App.jsx itself is now ~265 lines, just wiring the tabs together.

[DONE] `usedInRecipes(data, key)` centralized in lib.js. PantryTab.jsx's three
inline copies of `data.recipes.filter/some((r) => r.ingredients.some((i) =>
norm(i.name) === key))` — the rename-affected-recipes check, the remove-item
safety check, and the item 9 "used in" display — all call it now.

[DONE] Back to top on List, Ingredients and Meals. Both lists outgrow what anyone
will scroll back through by hand — the real catalog makes the Ingredients tab
about 8,500px — and search doesn't help: you search to FIND something, and are
then left wherever the list left you.
  A corner button, not a fourth control in the StickyBar. That row was cut down
  to search + one Filter button + the primary action precisely so it holds one
  line at 390px, and the bottom-right corner is under a thumb where the top of
  a phone screen is not.
  A scroll listener rather than StickyBar's IntersectionObserver, and the
  difference is the question each asks: the bar wants "has this element reached
  the top", which is what an observer reports, and this wants "how far down are
  we" — a distance an observer can only fake with sentinels at guessed depths.
  Passive, coalesced to one requestAnimationFrame.
  THREE THINGS ONLY A BROWSER SETTLES, all covered by
  tests/e2e/specs/backtotop.spec.mjs and all mutation-tested (never showing it,
  always showing it, and z-index above the dialog each fail exactly the right
  cases): it renders only past a scroll depth; it is position:fixed inside a
  page that already has a position:sticky bar creating a stacking context, so
  it can be painted but not tappable; and at z-index 20 it must lose to the
  dialogs at 70. The spec hit-tests the corner with elementFromPoint rather
  than trusting visibility, because "visible but something else owns that
  pixel" is exactly the failure that looks like a dead click handler.
  Unusually for that suite there is nothing persisted to assert on — this is
  pure UI, so the read-back rule doesn't apply.
  IT SHIPPED READING THE SIX LITERAL CHARACTERS \u2191, and the first version
  of this spec did not notice. JSX does NOT interpret a \uXXXX escape in a
  text child — only inside braces, which is why Section's chevrons were fine
  — so the button rendered gibberish while working perfectly. The test
  asserted the aria-label, the appear/disappear threshold, the hit test and
  the scroll, every one of which passed. THE LESSON IS NARROW AND WORTH
  KEEPING: behaviour assertions do not see the label, so anything a user
  READS needs asserting as text. The spec now checks the rendered glyph, and
  reintroducing the bug fails all three tabs.
  The List tab needed a fixture to be worth testing at all: it is empty in
  every other one, so the scroll test would have passed by never scrolling.
  longListState() in fixtures.mjs fills it with hand-added extras, which need
  no catalog, plan or recipes.

[DONE, HALF-DONE, and one half DELIBERATELY NOT STARTED] Items 39 and 40 — the
ingredient `note` field, the parser, and duplicate ingredients.

THE FIELD (item 39) is done. A recipe line is now { name, qty, unit, note? },
and `note` is ABSENT rather than empty when there isn't one, so every recipe
already stored keeps the exact bytes it had and an older build reading it back
has nothing new to carry. It is a property of THE LINE, not of the ingredient:
the same onion is diced in one recipe and sliced in another. It never reaches
aggregateItems' grouping key — addPart keys on ingredient + unit — so two
recipes wanting the same thing prepared differently still make one list row.
That is asserted in tests/e2e/specs/ingredientnotes.spec.mjs, because it is
the failure that would cost a duplicate purchase.
  WHAT IS NOT DONE, ON PURPOSE: the existing bracketed units in catalog.json
  ("can (15 oz)", "tsp (can omit)") are untouched, per the expand-then-contract
  note in item 39. The field ships and gets used for real first; moving that
  hand-written text is a separate data pass. It is information somebody
  deliberately recorded — it gets MOVED, not deleted.
  [DONE LATER — see "the migration" below.]

THE PARSER (item 40, first half) is done, and the rule that took two attempts
is where a comma is a note and where it is a list. "1 large onion, diced" is a
note. "1 1/2 lb ground chicken, pork, or turkey" is ONE ingredient with three
spellings, and splitting at the last comma produced the name "Ground chicken,
pork" — which matches no catalog entry, so it forks a new one. The rule is:
split on a comma only when there is exactly ONE, and never when the tail opens
with "or"/"and". Parentheses are taken from anywhere, and are read BEFORE the
unit word — "2 (14.5 oz) cans tomatoes" was reading `14.5` as the unit and
losing the can size with it.

COOKING STEPS now come out numbered, one per line. They were joined into a
single paragraph, so following them at the stove meant re-finding your place
each time you looked up. A numbered line STARTS a step and unnumbered lines
after it continue it (blogs wrap long steps), and they are renumbered from 1
rather than copied — a paste that starts at the second method starts at "5.".

DUPLICATE INGREDIENTS (item 40, second half — the valuable one) now ASK.
existingIngredientSuggestions() is the reverse of ingredientMatches: that one
answers "what am I part-way through typing", so it asks whether the known name
contains what you typed, and that question cannot see the failure that
actually happened — the EXISTING name was inside the PASTED one ("Olive oil"
inside "extra virgin olive oil", "Salt" inside "kosher salt"). So containment
runs both ways, plus a last-word check for "large onion" beside "Yellow
onion". Offered as a chip under the row, never applied: the importer forked
the catalog nine ways precisely by deciding instead of asking, and a wrong
guess accepted silently is how a catalog acquires three cilantros.
  MATCHING ON WHOLE WORDS, NOT SUBSTRINGS, is load-bearing and mutation-tested:
  "salted butter" contains the letters of "salt" and is not salt. A prompt
  that is usually wrong gets dismissed unread, and then so does the one that
  mattered. An exact match asks nothing for the same reason.
  It sits in the ingredient row itself rather than in a paste-review dialog,
  so it works for a hand-typed name too — the fork is not unique to imports.

TESTED: 184 unit tests, 76 e2e. Twelve mutations run; two survived and both
were equivalent (the empty-string note is already filtered by a second guard,
and startEdit's `...i` already spreads a stored note), and the e2e layer
caught a real one live — the note reached the editor and never reached the
catalog write, which no unit test could have seen.

[DONE] Item 47 — an over-long quantity broke the List row. REPRODUCED FIRST,
as the item asked, and it was worse than reported. At 390px, with a unit of
"28 oz can (San Marzano)":
    item-name button   0px wide
    "i" button         x = 421   (the screen is 390 wide)
    store select       x = 295, half off the screen
    page               scrollWidth 445 — the whole app scrolled sideways
The cause is that the quantity was `whiteSpace: nowrap`, so its min-content
width IS its full width and it cannot shrink. It sat on the row's single flex
line, and everything flexible was squeezed to pay for it — the name button
first, to nothing.
THE FIX IS A WRAPPING BOX, not a width. The name and the quantity now share
one flex container with `flexWrap: wrap`: they sit side by side when they fit
and the quantity drops to its own full-width line when they don't. The name
button is `flex: 1 1 0` — basis ZERO, deliberately, so it SHRINKS rather than
pushing the quantity onto a second line in the ordinary case. `flex: 1 1 auto`
was tried first and made every row two lines high, which the list pays for on
every item.
  `overflowWrap: anywhere` on both, because at 320px the quantity's box is
  63px and "Marzano)" is a single unbreakable 64px word — it spilled out the
  side without widening the page at all.
MEASURED ACROSS 14 COMBINATIONS (7 unit lengths x 320/390): no page overflow,
no text outside its own box, name button never below 44px, store select never
off screen. The ordinary "2 oz" row is unchanged at 91px/32px, one line.
TESTS: tests/e2e/specs/listrow.spec.mjs, and the assertion that matters is
scrollWidth vs clientWidth per ELEMENT, not per page. The first version of the
spec checked the page width and the hit tests, and a nowrap quantity inside a
shrink-to-zero wrapper passed both: the box was crushed to 59px and the text
ran out of it, which leaves the page exactly the right width. Mutation-tested
four ways; three fail the right tests. The fourth — removing the name button's
64px floor — is NOT independently covered, and honestly it is barely doing
anything: at 320px it is the difference between 64px and 63px. It is kept as a
statement of intent, not as something a test defends.
  The one thing this changed elsewhere: sides.spec.mjs found the quantity with
  `:scope > div > span`, which the extra wrapper broke. Now found by
  `span[style*="tabular-nums"]` — what the quantity IS, not how deep it sits.

[DONE] Item 44 — set an ingredient's store and aisle from the List tab. You
only ever learn what aisle something is in while standing in front of it, and
that meant leaving the list, opening Ingredients, and searching for the item
you had in your hand.
"Where it lives" now sits in the row's existing expand panel, and it calls the
SAME two writes the Ingredients tab calls — one setIngredientCfg each. That is
the point, not a detail: a parallel field would look right on this tab and
leave the Ingredients tab showing the old store forever, so the test drives the
List tab and then asserts on the INGREDIENTS TAB's dropdown.
  TWO DROPDOWNS, DELIBERATELY DIFFERENT, and the panel now says so. The one on
  the row is `overrides` — a reroute for TODAY. The one in the panel is the
  catalog — where the item lives from now on. Collapsing them would turn a
  one-off detour into a permanent wrong aisle on every future list.
  ONLY FOR AN INGREDIENT THE CATALOG HOLDS. A genuinely ad-hoc hand-added entry
  is keyed by its NAME, not by an ingredient id, so writing a store for it
  would mint a config entry under a key nothing else resolves. Those keep
  "Save to Ingredients", which mints the id properly.
  It is the List tab's first CATALOG write, so it is the first thing here a
  guest cannot do. Hidden, not disabled — same rule as everywhere else.
FOUND WHILE LOOKING AT IT, in a screenshot rather than in a test: the panel
read `Matches ingredients named "ing_2ym41inb"`. True when the key WAS the
name; once recipe lines started storing ingredient ids it became gibberish
sitting above an explanation of case-insensitive matching that no longer
happens. An item with a catalog entry is matched by identity and needs no
sentence at all; only an ad-hoc entry is still matched by its spelling, and
only it still says so — by name, never by key. Asserted as TEXT: every
behaviour assertion around that line passed, exactly like the ↑ button.
TESTS: tests/e2e/specs/listedithome.spec.mjs, mutation-tested five ways — a
parallel field, the panel collapsed into the override, the aisle stored without
its store, the guest given the editor, and the raw id put back — each fails the
one test written for it.

[DONE] Item 45 — the tab bar no longer scrolls away. It lived in the header, so
switching tabs from halfway down the Ingredients list (about 8,500px against
the real catalog) meant scrolling all the way back to the top first.

PINNED TO THE BOTTOM, NOT THE TOP, and the reason is the two things already
fixed up there. The per-tab StickyBar is stuck to `top: 0`; a pinned header
would need it to carry a hand-maintained offset that nothing enforces. And the
five labels wrap onto two lines at 390px, so pinning them at the top costs
about 90px of permanent chrome ABOVE the search bar. At the bottom it costs one
54px row, and it is under a thumb rather than at the far end of the phone.

THREE PLACES HAVE TO AGREE ABOUT THE BAR'S HEIGHT, which is why BOTTOM_NAV_H
is a constant in theme.js rather than a number written three times: the bar
itself, the page's bottom padding (or the last row hides under it forever, and
the page just looks like it ended there), and the back-to-top button (or the
two controls sit in the same corner and a tap meant for "List" scrolls you to
the top instead).

THE LABEL SIZE IS MEASURED, NOT CHOSEN. "Ingredients" is 68px at 12px type and
each tab gets 64px at 320px, so it ellipsised to "Ingredien…". Options were
measured across 5 font sizes x 3 letter-spacings: 11px only fits with zero
margin, which is one font-load away from clipping again. `clamp(10px, 3.1vw,
12px)` shrinks the label only on the phones that need it — 10px at 320, 11.6px
at 375, 12px from 388 up. Verified with scrollWidth vs clientWidth at 320, 360,
375, 390 and 430.

THE FIRST TEST OF THIS WAS WRONG AND MUST NOT COME BACK. It scrolled to the
bottom of a long list and asserted the bar sat on the viewport's bottom edge —
which a bar that had scrolled away with the page ALSO does, because at the end
of the document it is the last thing on screen. `position: static` passed it.
It now measures at THREE scroll depths and asserts the bar's viewport position
does not change; two positions cannot both be the bottom.
Five mutations, all caught: unpinned, above the dialogs, no bottom padding,
back-to-top dropped onto the bar, and a fixed label size.

[DONE] The migration — a modifier stranded in `unit` moves into `note`. This
is item 39's "later pass", and the trigger was a real shopping list reading:
      Garlic   16 cloves (2 chopped, 6 whole) + 11 cloves
`unit` is HALF THE SHOPPING LIST'S GROUPING KEY, so one recipe spelling it
"cloves (2 chopped, 6 whole)" against eleven saying "cloves" is arithmetic,
not tidiness — two rows of garlic that cannot add up. 38 lines across 14 of
the 22 recipes were affected.

THE THING WORTH REMEMBERING IS WHERE IT HAD TO RUN. The first attempt at this
class of fix (#90) edited public/catalog.json, and the next export brought
every duplicate straight back. THE FILE IS EXPORT ONLY: App.jsx reads it once,
through seedCatalog, and only when a household has NO catalog of its own
(`hCatalog || seedCatalog(catalog)`). Once the database has one, the file is
never consulted again. Editing it fixes the git history and nothing a phone
will ever see. Anything that has to reach the data has to run in the app.
  So it runs in two places, and needs both: on the catalog-adopt path beside
  migrateToIngredientIds, which is what fixes an EXISTING household; and
  inside seedCatalog, so a NEW household is never born with the bug. The
  second is also the only one an e2e test can reach — the local-only build
  compiles sync out, so the adopt path has unit tests and a proven sibling
  pattern behind it and nothing more. Said plainly rather than implied.

IT REUSES splitIngredientNote rather than defining "what is a note" twice,
which makes it conservative by inheritance: no brackets and no trailing
clause means untouched. That matters more than the cases it does catch —
`stick` (from "4 carrots cut into sticks") is a WRONG UNIT, not a note, and
no rule here can tell. It stays for a person to fix, and so does
"skin-on, bone-in", which this only half-improves.

GATED AND IDEMPOTENT, and the gate is not decoration: without needsUnitNotes,
every launch on both phones would push the whole catalog back and bump
updatedAt forever. Skipped for a guest, whose catalog writes the rules refuse
anyway — the attempt would raise the "you're a guest" banner on a screen they
only opened to shop.

APP_DATA_VERSION IS NOT BUMPED. An older build carries `note` through
untouched everywhere except a recipe it actually re-saves, which is the same
exposure #94 already shipped with. A field an old build can carry is not a
reason to lock a phone out.
Five mutations, all caught: no migration, text deleted instead of moved, an
existing note overwritten, the gate always true, and unknown fields pruned.

49. [DONE] THE TAB BAR STRANDED ITSELF WHEN THE KEYBOARD OPENED. Reported
    from a real iPhone with a screenshot: the bar sat in the middle of the
    screen, page content visible BELOW it, with a gap between it and the
    keyboard.
    THE CAUSE IS WHAT `position: fixed` IS FIXED TO. It is fixed to the LAYOUT
    viewport, and iOS Safari does not shrink that when the keyboard opens — it
    shrinks the VISUAL viewport and scrolls the layout one underneath. So a
    bottom bar stops tracking the bottom of what you can see. This is a
    property of every fixed bottom bar on iOS, not of this one.
    HIDDEN, NOT REPOSITIONED, and that was a choice. Repositioning off
    visualViewport is possible and is a known rabbit hole — the numbers settle
    over several frames, differ by iOS version, and the result parks the bar
    directly on the keys, where it eats typing room and catches taps meant for
    the top keyboard row. Nobody switches tabs mid-word. It comes back the
    moment the keyboard closes.
    THE PAGE KEEPS ITS BOTTOM PADDING while the bar is hidden. Reclaiming it
    would reflow the list under the cursor every time the keyboard opened,
    which is a worse bug than the one being fixed.
    THE THRESHOLD IS THE ONLY REAL DECISION. iOS also shrinks the visual
    viewport by 60-90px when the URL bar collapses on scroll; counting that as
    a keyboard would make the bar flicker away every time you scrolled the
    shopping list. A keyboard is 250px+. 150 sits in the gap, and the test
    pins BOTH ends of it so it cannot drift either way.
    The back-to-top button hides with it — it is anchored above the bar, so
    leaving it behind would strand it in the same gap.
    WHAT IS NOT VERIFIED, PLAINLY: the iOS keyboard cannot be opened in the
    browser the e2e suite drives, so nothing here proves the fix on a phone.
    What IS tested is keyboardIsOpen against real viewport numbers, including
    the URL-bar case, and the other direction in the browser — that the bar
    does NOT hide when there is no keyboard. That second one matters most:
    getting it wrong removes the navigation permanently, on every device.
    Four mutations, all caught. The unconditional-hide one is caught by the
    HARNESS rather than an assertion — openApp waits for a tab button, so the
    whole suite times out. Loud, if slow.

UI density — Ingredients row [DONE], List row [DONE].
The List row carried a checkbox, the name, the quantity, a 118px store
dropdown and an "i" button on one line at 390px. That crowding is what
produced the over-long-unit bug (item 47): everything flexible was squeezed
to pay for a quantity that would not shrink.
THE STORE MOVED INTO THE PANEL, and the row is now checkbox / name / quantity
/ i. The name went to 15px with it — spending some of the 118px back on the
one thing you actually read while shopping is the whole point.
ONE CONTROL NOW, AND IT ASKS. There were two store dropdowns — the row's
writing `overrides` (today) and the panel's writing the catalog (from now on)
— and the FAQ needed an entry to explain which was which. Two fields for one
question is the app admitting it does not know what you meant, so it asks
instead: "Just this trip" or "Always". That FAQ entry is now a different
question, because the old one no longer describes anything.
  "Always" ALSO CLEARS THE REROUTE. Today's detour outranks the default, so
  leaving it would make the change look like it did nothing until the trip
  ended. Mutation-tested — it is the kind of omission that reads as a bug in
  the store picker rather than in the write.
  NOBODY IS ASKED A QUESTION THEY CANNOT ANSWER. A guest has no catalog write,
  and an ad-hoc hand-added entry is keyed by its name rather than an
  ingredient id so it has nowhere to keep a default. Both get the one answer
  that exists, with no dialog. They keep the control itself: a reroute is a
  LIST write, which is exactly what the guest role grants.
THE STORE STILL SHOWS ON THE ROW IN A-Z VIEW, where no heading carries it —
keyed to the VIEW, not to showAisle. showAisle is about store-flow ordering
and is false in by-store A-Z, which is precisely where the heading is; using
it printed "Aldi" on every row under an "Aldi" heading. Caught by looking at
a screenshot, not by a test.
DISCOVERABILITY IS THE KNOWN COST: the store now takes a tap to reach, and
nothing on the row advertises the panel. Accepted deliberately — it is a few
taps a shop against 118px on every row of every list.
The Ingredients gear panel had become a flat pile of unrelated concerns, and
its aisle grid rendered one input per store (5, shown even where the item
isn't sold), so adding a store widened every ingredient's panel. Fixed by:
  - showing only the default store's aisle, with the rest behind an
    "Aisles at N other stores" reveal — the panel no longer grows with the
    store list
  - grouping the panel into "Where it lives" (store + aisles) and "What it
    is" (staple, used-in, rename) instead of one flat stack
The Meals and Ingredients control rows also collapsed to search + a single
Filter button + the primary action, which fits one line on a phone.

[DONE, item 104] The List row carried the same pressure and got the same
treatment: the store moved into the details panel and the name grew.

52. [DONE] THE SYNC STATUS WAS DRAWN THROUGH THE "HOUSEHOLD" HEADING, AND SAID
    NOTHING USEFUL WHEN IT DID. Reported from a real phone as a screenshot:
    "Sync error — changes may not be saved" in red, overlapping the heading it
    sits beside on the Settings tab.

    TWO FAULTS, ONE CAUSE — a flex item told it may shrink to nothing, with
    text that carries on being painted anyway.
      - Section's title span had `minWidth: 0`. At 320px the heading was
        squeezed to 0px wide while its 82px of text kept rendering, straight
        over the status. Removing it lets the flex default (min-width auto)
        hold the title at its longest word; the status wraps instead, which is
        what should give way. `overflowWrap: break-word` is the last resort
        for a single word wider than the card — it does not change the
        min-content width, so it cannot reintroduce the squeeze.
      - The header's <h1> and the status were both shrinkable, so the longest
        status broke "Grocery Run" across two lines. The app's own name is not
        the thing that should move for a transient message: flexShrink 0.
    The Settings status also renders now when sync is OFF ("Saved on this
    device"), which the header already did. It is a status either way, and it
    is what makes the layout testable at all in a local-only build.

    "SYNC ERROR — CHANGES MAY NOT BE SAVED" IS TRUE AND USELESS. There was no
    way to find out more, because there was nothing left to find: sync.js
    reported the failure as a bare `true`. Which write, and which of the two
    very different causes, had already been thrown away — and a phone has no
    console to read the discarded line from.
    watchWriteErrors now reports { where, code, message } or null. `where` is
    plain words at the six call sites ("recipes and ingredients", "shopping
    list and week plan", ...); `code` is the database's own, or e.name for the
    other shape that lands there — a payload the SDK refuses before the
    network, which throws an ordinary Error rather than PERMISSION_DENIED.
    writeErrorAdvice (lib.js, pure, tested) turns that into the sentence, and
    the split is the point: PERMISSION_DENIED means the ACCOUNT is wrong — a
    guest, the wrong account, or removed — and the fix is a new invite;
    anything else means the app sent something malformed and re-inviting a
    phone fixes nothing. One message for both sends somebody to the wrong
    place.

    STATUS_PREVIEW_KEY is a second e2e seam alongside GUEST_PREVIEW_KEY, read
    only when syncEnabled is false. A sync-stripped build can only produce the
    SHORTEST of the seven statuses, and the longest is the one that broke the
    layout. It names a status; App turns it back into a label through the real
    syncIndicator, so no test can assert on wording the app does not show.
    Measured, not looked at, for listrow.spec.mjs's reason: text overflowing a
    zero-width box leaves the page the right width and every element
    "visible". The per-element invariants are what catch it — does each box
    contain its own text, do two boxes claim the same pixels — plus a Range
    per line for the <h1>, since box height also grows for padding and font.
    Mutation: both fixes reverted together, 6 of 9 assertions failed, with
    "the title text needs 82px and was given 0px" naming the reported bug
    exactly. The three that still passed are the shorter status at the wider
    screen — which is why the longest string is the one under test.

    WHAT IS NOT ANSWERED: why that phone got a refused write in the first
    place. Nothing recorded it at the time, which is the whole reason for the
    change above. The next occurrence will name the write and the code.

53. [DONE] AN ITEM CALLED "DR. PEPPER" BROKE SYNC PERMANENTLY. Found chasing
    item 52's report — "it happened after checking off a bunch of stuff, then
    Done shopping, then coming back later".

    The list keys a hand-added item by its own NAME, `norm(name)`. RTDB refuses
    `.` `#` `$` `[` `]` in a key, so state/list/extras/dr. pepper made the
    Firebase SDK throw before the write ever left the phone:
      "values argument contains an invalid key (dr. pepper)"
    VERIFIED AGAINST THE REAL firebase PACKAGE, not reasoned about. `.` `#`
    `$` `[` `]` throw; `%` and `&` are accepted, so "Milk 2%" and "Ben &
    Jerry's" were never the problem. `/` IS WORSE THAN REFUSED: it is
    accepted, and "1/2 gallon milk" silently writes a node "1" containing
    "2 gallon milk". No error, wrong data.

    IT IS PERMANENT, AND THAT IS THE PART THAT MATTERS. A failed write keeps
    its baseline on purpose (item 21) so nothing is dropped from a future
    diff — so the bad path is re-sent on EVERY write from then on. The error
    never clears, and reopening the app does not help: the key is in the
    device's cached state. That is the shape of the report exactly — it does
    not matter which tap first created the key, only that nothing afterwards
    could succeed.
    IT ALSO EXPLAINS WHY IT WAS NOTICED AFTER A SHOP rather than at the tap.
    The status line is 12px at the top of the header; you are looking at rows
    while you shop.

    TWO LAYERS, AND THEY FAIL INDEPENDENTLY — each has its own mutation:
      - keyForName at the two places a typed name becomes a key (adding an
        ad-hoc item, renaming one). Prevention.
      - withSafeKeys in normalizeLocal, over list.checked / .overrides /
        .extras / .bought and stapleNeeds. A phone that already holds one is
        stuck until its own copy is fixed, and the app can no longer produce
        that state, so healing is the only way those phones recover.
    NO EXPAND-THEN-CONTRACT, and the reason is worth keeping: the database
    NEVER RECEIVED these keys — the write failed every time — so there is no
    shared copy to diverge from. The one exception is `/`, which did get
    written, as nested nodes.
    THE HEAL REACHES STORAGE ON THE NEXT WRITE, not on the read. Nothing
    re-saves an unchanged state. It is in memory immediately, which is what
    the sync layer writes from, so the first write after this build loads
    already carries the clean key.
    safeKey NEVER CHANGES CASE. It runs over keys that already exist,
    ingredient and recipe ids among them; lowercasing a legal key would
    orphan everything pointing at it. Mutation-tested from both sides:
    stripping nothing (6 unit tests fail), and stripping `%`/`&` as well (the
    "characters the database accepts are left alone" test fails).
    COLLISIONS ARE MERGED, NOT DROPPED: "Dr. Pepper" and "Dr Pepper" collapse
    onto one key, so `checked` ORs and `bought` sums. Losing a tick or a
    banked quantity silently would be a second bug inside the fix.

    THE TEST THAT WOULD HAVE CAUGHT IT is on planWrite, not on a tab:
    everything the app writes goes through it, so asserting that no path it
    produces has a refused segment covers every screen at once. Plus three
    browser tests that drive the real add / tick / Done shopping sequence and
    read back what was PERSISTED — the row rendered perfectly on the broken
    build, the item just never left the phone.

    THE SAME CLASS IN THE CATALOG, fixed with it: `aisles` is keyed by STORE
    NAME, so a household shopping at "H.E.B." would have had every catalog
    write refused — the recipes and ingredients simply stop saving, same
    permanent stuck error, different node.
    DIFFERENT FIX, because a store name is DISPLAYED. It is typed once and
    then shown on every heading and in every dropdown, so it cannot be quietly
    rewritten the way an invisible key can. The name stays exactly as typed in
    `catalog.stores`; only the aisles map's key is derived, by aisleKey.
    NO CASE CHANGE, and that rule needed its own test rather than being left
    to the ones that read an aisle back: reads survive either way, because
    normalizeCfg puts the STORED map through aisleKey too and both sides then
    agree whatever it does. What the rule buys is that every store name that
    works today keys to ITSELF, byte for byte — upgrading writes nothing at
    all, where lowercasing would rewrite every ingredient's aisles map on the
    first catalog edit and churn 146 entries through the next exported
    catalog.json. Mutation-tested both ways: the raw name (1 unit + 1 browser
    test fail) and norm() (2 unit tests fail, every browser test still green).
    THE COST, WRITTEN DOWN: two stores whose names differ only in those
    characters — "H.E.B." and "H E B" — share one aisles entry per ingredient.
    A strange pair of stores to have, and better than a catalog that silently
    stops saving.

54. [DONE] "RESTORE STARTER CATALOG" ORPHANED THE SHOPPING STATE, AND THE
    ORPHANS LISTED THEMSELVES AS GROCERIES. Reported as a screenshot: eight
    rows in the already-bought panel reading "Ing_05jz04l4 · 1", in among
    Hummus and Jumbo shrimp.

    seedCatalog MINTS A FRESH ID FOR EVERY INGREDIENT — it calls
    ensureIngredientId, which is uid()-based — so the moment a restore ran,
    everything the state keys by id pointed at an ingredient that no longer
    existed: what was ticked, what an earlier trip already bought, today's
    store reroutes, which staples you are out of. This was KNOWN and written
    down as a cost of the restore. What it was not was harmless: an orphan can
    never resolve later, because nothing on any list carries that id again, so
    those entries sit in the panel permanently.

    TWO FAULTS, AND THEY NEEDED SEPARATE FIXES:
      - THE CAUSE. remapStateIngredientIds moves the state onto the new ids,
        matching BY NAME — the only thing the two catalogs share. Called from
        restoreStarter with the OLD config, read before updateCatalog replaces
        it, since that is the only copy of the old ids' names.
        An entry that resolves to nothing is DROPPED here, which is the
        opposite of remapIngredientKeys and deliberate: there an unresolved key
        was stale and cost nothing to keep, here it is an id whose ingredient
        is deleted outright and keeping it IS the reported row. A hand-added
        entry survives either way — it carries its own name, so it goes back to
        being ad-hoc rather than disappearing off the list.
        Collisions merge (checked ORs, bought sums): a restore collapses
        duplicates a household had merged apart, and losing a banked quantity
        would silently put something back on the list.
      - THE DISPLAY. ingredientNameFor ended in cap(key), which was right for
        exactly as long as the key WAS the name. A minted id is not a name, so
        it now returns "" for one it cannot name — a decision reversed from an
        earlier note in the test that said "something deleted still shows
        SOMETHING rather than blank". The screenshot settled it. A name-key
        still returns itself.
        The panel groups what it cannot name into one row that says what they
        are and offers Clear. Phones that already ran the old restore still
        hold these, and the names are UNRECOVERABLE — the catalog that had
        them is gone — so explaining and clearing is the whole of what can be
        done for them.

    THE PANEL'S COUNT IS OVER EVERY ENTRY, nameable or not. Splitting the
    rows in two made it easy to count only the named ones, which reads
    "0 items already bought this week" as the header of a panel listing
    thirty. Caught by a second screenshot, from a phone where EVERY entry was
    an orphan — the case the first screenshot did not contain.

    ONE update() CALL FOR THE CLEAR, not one per key. update() snapshots
    localRef.current, which only refreshes on the next render, so a forEach
    over unbuy() rebuilds from the same stale base every time and clears
    exactly one — with the button looking like it worked. The file says this
    at the top of update(); it is easy to write anyway.

    Three mutations, each caught by a different test: the restore not
    remapping (2 browser tests), ingredientNameFor back to cap(key) (1 browser
    + 1 unit), and the Clear button back to one update() per key (1 browser).
    The unit tests cover the merge and the forward-compat spread; the browser
    tests assert on the PERSISTED state, because a fix that only stopped
    rendering these would leave a list quietly subtracting quantities that
    belong to ingredients nobody can name.

55. [DONE] THE ACTUAL CAUSE: A UNITLESS ITEM COULD NOT BE BANKED. Reported
    twice, weeks apart, with the same sequence and nothing else in common —
    "checked off a bunch of stuff, hit Done shopping, came back later" and
    "checked everything off, then hit done shopping and the sync broke".

    `bought` is keyed by ingredient AND THEN BY UNIT: { ing_x: { lb: 2 } }. A
    unitless item — "Lemon · 1", "Large potatoes · 4", which is most of a real
    list — has the unit "", and an empty string is not a legal RTDB key.
    Reproduced against the real firebase package, driving the paths diffPaths
    actually produces:
      update failed: values argument contains an invalid key ()
      in property '...state.list.bought.ing_3jskfrr8'.
      Keys must be non-empty strings
    Done shopping is the ONLY thing that writes `bought`, which is why that one
    press is where it always appeared, and — since a failed write keeps its
    baseline (item 21) — why nothing after it could succeed either.

    THE ITEM-NAME BUG (53) WAS REAL BUT WAS NOT THIS. Their household's
    catalog has no refused character in any of 146 ingredient names, 5 stores
    or any unit. The empty unit needs no unusual data at all: every household
    hits it the first time it finishes a shop with one unitless item on the
    list.

    THE TEST THAT SHOULD HAVE CAUGHT IT DID NOT, and that is the lesson worth
    keeping. Item 53's planWrite test checked the PATH SEGMENTS of everything a
    flush sends — and passed, every run, on the build that was breaking a shop
    every week. `bought` is written as one object per ingredient, so the unit
    keys travel INSIDE A VALUE, never as a segment. Firebase validates those
    exactly as strictly. The test now walks the values too, which is the
    version that fails on the old build.

    THE STORED SHAPE IS THE STORABLE ONE. unitKeyFor turns "" into NO_UNIT_KEY
    on the way in; the two reads that care — groupPartsByDimension and
    qtyLabel, already the single funnels for "what unit is this" — turn it back.
    Translating at the sync seam instead was considered and rejected: the write
    baseline and the in-memory state would then disagree about their own shape,
    which is a subtler bug than the one being fixed.
    THE SENTINEL IS "_" and could collide with somebody typing `_` as a unit.
    Not a case worth a mechanism: unitInfo has never known it, so it has never
    added up with anything.
    HEALING IS THE OTHER HALF. Every phone that pressed Done shopping on the
    broken build has `{ "": 4 }` in its cache and is re-sending it on every
    write; normalizeLocal rewrites it, and merges rather than overwrites, since
    a household mid-upgrade can hold both "" and "_" for one ingredient.
    THE SUPPRESSION IS WHAT THE FIX COULD HAVE BROKEN QUIETLY: a stored key
    that no longer matched the need would make every write succeed and the
    cupboard silently stop working. That has its own test, in both directions.

    Three mutations, each caught by a different test: the raw unit written
    again (1 browser test), the healing removed (2 unit tests), and the
    sentinel never mapped back (2 unit tests — display and suppression).

56. [DONE] THE STANDING KEY AUDIT, and the sweep that produced it. Item 55
    shipped past a test written to prevent exactly it, so the question was no
    longer "is this one fixed" but "what else is there".

    EVERY DYNAMIC KEY THE APP CAN WRITE, walked by hand and then pinned by a
    test. Both nodes go through planWrite, so one audit covers everything:
      state/list/{checked,overrides,extras,bought}, stapleNeeds
                              ingredient keys — keyForName / minted ids  [55/53]
      state/list/bought/*/*   THE UNIT — the one that was breaking shops  [55]
      state/list/selections   recipe ids: uid(), or the shipped file
      state/plan/*/*          DAYS and MEAL_TYPES constants; now healed anyway,
                              because an imported backup is hand-editable
      catalog/ingredients/*   minted ing_ ids
      catalog/ingredients/*/aisles/*   STORE NAME — aisleKey             [53]
      catalog/recipes/*       uid(), or the shipped file
      catalog/stores, recipes/*/ingredients   arrays, written atomically
      members/$uid, invites/$token, users/$uid, $code   uid / generated / cleanCode
    Nothing else in the app writes a key it did not mint or derive.

    THE AUDIT TEST IS THE POINT, not the sweep — a sweep is only true on the
    day it is done. Three things it does that the item-53 test did not:
      - IT WALKS VALUES, NOT JUST PATHS. `bought` is written as one object per
        ingredient, so its unit keys never appear as a path segment. That is
        precisely why item 55 was invisible for weeks.
      - IT COVERS BOTH WRITE SHAPES. planWrite returns an `update` once there
        is a baseline and a `set` — the WHOLE node — before there is one,
        which is the first write of every session.
      - IT CHECKS UNWRITABLE VALUES TOO. Verified against the real firebase
        package: NaN, Infinity, undefined and functions all throw with the
        same permanent-failure shape as an illegal key. -0 and Date are
        accepted. Every Number() in the app was checked; all are guarded by a
        `> 0` or `=== ""` test except normalizeCfg's legacy `aisle`, where
        Number("aisle 4") is NaN — now dropped rather than written.
    It also audits the SHIPPED public/catalog.json, which is hand-edited and
    pasted back by "Restore starter catalog": a store name or recipe id typed
    into that file with a "." in it would break every catalog write for every
    household on the next release.
    The test asserts its own control — that it still FAILS on a raw empty key
    and on a NaN — because an audit that cannot fail is worse than none.
    Mutation: reverting keyForName, unitKeyFor and aisleKey together fails 9
    tests, the audit among them, naming the empty key at its exact path.

57. [DONE] THE THREE UX FINDINGS THAT WERE NOT MATTERS OF TASTE (51a-c).
    Each was a number the page reported about itself, so each has a test that
    reproduces the number.

    51a. THE MOST DESTRUCTIVE CONTROLS HAD THE SMALLEST TARGETS. "Remove
    <store>" was 11x15px — it unassigns every ingredient that lived there —
    and "Delete <recipe>" was 21x24. Both a third of the 44px a thumb reliably
    hits, both the smallest thing on their screen, both beside something
    harmless.
    THE PILL GREW RATHER THAN THE BUTTON OVERFLOWING IT, and that is the only
    interesting decision here. Expanding a small button past its container
    with negative margins is the usual trick, and it is WRONG for these: the
    store pills wrap and sit 8px apart, so the hit areas would overlap and a
    mis-tap would offer to delete the WRONG store. The test asserts
    non-overlap as well as size, and mutation-testing the negative-margin
    version fails it by name ("Remove Costco" and "Remove Schnucks" overlap).
    The Meals ✕ is absolutely positioned, so growing it to 44 costs the layout
    nothing; it sits in the card's corner with padding so the glyph did not
    move.

    51b. FOUR TEXT COLOURS HAD FAILED WCAG AA SINCE THE APP WAS WRITTEN, two
    of them the primary action on their tab ("Done shopping", "Start a new
    plan" at 4.09:1). All marginal — 4.06 to 4.33 against a 4.5 floor — which
    is exactly why nobody was ever going to see it by looking.
    THE INK DARKENED, NOT THE BACKGROUND LIGHTENED, so the soft-pill treatment
    is unchanged: same hue, same saturation, lower lightness.
      tomato #C2452D -> #B4402A   4.09 -> 4.62 on tomatoSoft
      gold   #8A6D1D -> #83681C   4.26 -> 4.61 on goldSoft, 4.49 -> 4.86 on paper
      faint  #6B7263 -> #63695B   4.06/4.15/4.33 -> 4.62/4.72/4.93 on the three
                                  soft backgrounds, 4.57 -> 5.20 on paper
    `faint` was failing on ALL THREE soft backgrounds; the evaluation only saw
    greenSoft because that is what happened to be on screen. Arithmetic found
    the other two.
    contrastRatio lives in lib.js — pure WCAG 2.x maths — because theme.js is
    values only and a test needs something to compute with. The test walks
    every pair the app actually renders and reports the failures as a list, so
    a regression names the colour and the number. It also checks the formula
    against known values (black/white = 21, #767676 on white = 4.54), because
    a broken formula would pass everything silently.

    51c. THE INGREDIENT SUBTITLE CLIPPED AT 320px: "Grocery store · aisle 8"
    needs 132px and was given 108, and nowrap+ellipsis cuts from the RIGHT, so
    the store name — the thing you scan the row for — was the half that went.
    It wraps now. A second line on a narrow phone is cheaper than losing the
    name, and it costs nothing at 390px, where it still fits on one.

    THE HIT TEST HAD TO LEARN TWO THINGS ABOUT THIS APP. elementFromPoint only
    answers for the visible viewport, and Ingredients is 12 screens long, so
    "below the fold" is not a failure; and the fixed tab bar covers the bottom
    of the viewport BY DESIGN, with matching page padding, so a control under
    it is not unreachable either. The bar's own bounding box is read rather
    than BOTTOM_NAV_H being hardcoded a fourth time. Size and overlap are
    still checked for every control, on screen or not.
    Five mutations, all caught: both old target sizes, the old truncating
    subtitle, the negative-margin overlap, and a lightened `faint`.

58. [DONE] THE THREE THINGS THE UX EVALUATION SAID IT HAD NOT MEASURED.
    Item 51 ended "no VoiceOver pass, no one-handed reach testing, no timing
    of a real shop". All three are now measured. Two produced real defects,
    one came back clean, and two findings are decisions rather than faults.

    A. THE MECHANICAL HALF OF A SCREEN-READER PASS. Driven on all five tabs
    at 390x844 against the real catalog. STILL NOT A VOICEOVER PASS on a real
    iPhone, and the difference matters: this proves the app is not SILENT, not
    that it is pleasant to use by ear.
      - THREE TEXT INPUTS HAD NO ACCESSIBLE NAME: both "Add …" fields on
        Ingredients and "Add shopping item" on List. A PLACEHOLDER IS NOT A
        NAME — VoiceOver falls back to it, but it is gone the moment there is
        a value in the field, and "Add shopping item" carries role="combobox",
        which announced nothing at all.
        (Recorded as six at the time. The three SEARCH boxes already had
        aria-labels and were never flagged by the audit; labels were added to
        them anyway, which left a duplicate attribute on each. Removed.)
      - ZERO LIVE REGIONS IN THE ENTIRE APP. The sync status changes with
        nothing the user did — "Synced" to "Sync error — changes may not be
        saved" — so there is no reason to go and look at it, and every
        confirmation ("Copied", "Imported.", "Starter catalog restored.",
        "Sent to …") appears and vanishes. role="status" (polite) on all of
        them. Polite, not assertive: none of it is urgent enough to cut across
        a sentence, and "Synced" interrupting would be worse than silence.
      - THE HEADING STRUCTURE WENT h1 THEN h3, and Section's title is a
        BUTTON — so the Settings tab had exactly ONE heading, the app's name.
        The five tab-level h3s became h2, and Section's button now lives
        inside an h2 (heading + button is the standard disclosure pattern,
        zero margin so nothing moves).
      - THE MEALS A-Z VIEW HAD NO HEADINGS AT ALL: 22 recipe cards, titles
        rendered as buttons. The card title is now a heading too, and ITS
        LEVEL FOLLOWS THE VIEW — h3 grouped, where a meal-type h2 sits above
        it, h2 in A-Z, where nothing does. A fixed h3 skips a level; that is
        mutation-tested.
    The test computes the accessible name the way a screen reader roughly
    does and DELIBERATELY DOES NOT CONSULT THE PLACEHOLDER. Playwright's own
    accessibility snapshot resolves placeholders into names and reports all
    six of those fields as fine, which is why it is not used here.
    Four mutations, four different tests: labels removed, the status no longer
    live, Section back to a bare button, the card heading level fixed at h3.

    B. ONE-HANDED REACH, measured on a 390x844 phone: where each control sits,
    unscrolled, split at 62% and 33% of the viewport height.
      List        11 controls   5 easy   0 mid   6 stretch
      Meals       22            13       5       4
      Week plan   18            11       4       3
      Ingredients 24            17       2       5
      Settings    10            5        2       3
    Most of the stretch-zone controls are set-and-forget (the view toggles at
    y=125, the section headers). ONE IS NOT: "Done shopping" at y=202, which
    is pressed at the end of a shop, one-handed, holding a trolley. That is a
    layout decision, not a defect, and is open.

    C. WHAT A WEEK ACTUALLY COSTS, in the two things that can be counted.
    Four dinners planned from the real catalog: 9 taps. The list that comes
    out is 23 rows, 1615px, 1.9 SCREENS at 844px tall. The shop itself is 25
    taps (one per row, plus Done shopping and its confirmation). 35 taps for
    the week, start to finish.
    THIS CAME BACK CLEAN and is recorded so nobody optimises it on instinct:
    1.9 screens for a full week's list is not a scrolling problem, and 35 taps
    is not a tapping problem. Contrast with item 51e's Ingredients tab at 12
    screens — that one IS long, which is what search and back-to-top are for.

    D. `maximum-scale=1` IN index.html BLOCKS PINCH-ZOOM ON ANDROID. Recorded
    here as a trade-off and NOT changed at the time; item 61 fixed it at the
    source instead, which turned out to be the better answer.

59. [SUPERSEDED BY ITEM 63] 51d: THE WEEK PLAN SHOWS ONLY THE MEAL TYPES YOU
    ACTUALLY PLAN. Kept for the reasoning and the mutations; the SHAPE it
    describes lasted two changes.
    4 types x 7 days is 28 slots and a household that plans dinners fills 4-7;
    reading four dinners took 2.5 screens, and Sunday alone filled one to show
    a single meal. Now 0.9 screens for the same four.

    HIDING TYPES, NOT DAYS, and that was the choice. The emptiness is almost
    entirely along the TYPE axis rather than scattered, so collapsing empty
    days would have produced a ragged layout for much less. Offered both ways
    with mockups; this is what was picked.
    A TYPE IN USE ON ANY DAY SHOWS ON EVERY DAY. The tab's whole value is that
    a slot is always in the same place — a row that came and went per day
    would be a worse trade than the scrolling it saves.
    AN EMPTY WEEK STILL SHOWS ONE ROW (Dinner), or there is nowhere to plan
    into and the tab reads as broken rather than compact. Mutation-tested: the
    version that collapses to nothing fails.
    NOTHING IS STORED about which types you plan. `showAllTypes` is view state
    and planning a breakfast is what makes Breakfast stay — so there is no new
    preference to sync, migrate, or disagree about between two phones.
    THE REVEAL IS KEYED TO typesInUse, NOT TO THE TOGGLE. Keyed to the toggle
    the control removed ITSELF the moment it was used, which made the extra
    rows a one-way door for the rest of the session. Caught by the test, not
    by reading it.
    dayHasMeals STILL READS ALL FOUR TYPES: the green border is what says "this
    day has something" at a glance, and deriving it from the VISIBLE rows would
    make a day whose only meal is hidden look empty.

    THE COST, ACCEPTED AND WRITTEN DOWN: planning a lunch on a fresh week now
    takes one extra tap. The e2e helper was changed to take that tap rather
    than the suite quietly losing its coverage of the other three meal types —
    a test that routes around a new step stops describing the app.

    ALSO DECIDED, NOT CHANGED: "Done shopping" sits at y=202 on an 844px
    screen, in the hardest quarter to reach one-handed, and it is pressed at
    the end of a shop while holding a trolley. Left where it is deliberately —
    it is once per trip, already behind a confirmation, and moving it down by
    the tab bar puts a destructive control under the thumb that is ticking
    items off for the whole shop.

60. [DONE] THE TAB BAR READ AS A FOOTNOTE. Reported from real use: the only
    navigation in the app was easy to miss. It was 12px at weight 500 with the
    unselected tabs in `faint` — the colour used for secondary text you are
    meant to SKIM PAST, which is the opposite of what navigation should be.
    Weight 700, and the unselected tabs in `ink`. The selected one stays green
    and keeps the soft background and the 3px rule doing that work.

    BOLD IS WIDER, AND THAT COST A RE-MEASURE. The old clamp(10px, 3.1vw,
    12px) was measured at weight 500; at 700 it ellipsised "Ingredients" at
    320px AND 390px the moment the weight changed. The existing test caught it
    on the first run, which is the whole reason that assertion exists.
    "Ingredients" needs 6.49px of width per 1px of type at weight 700, and
    each of five tabs gets (viewport/5 - 2)px: 62px at 320, 76px at 390.
    clamp(9px, 2.85vw, 11.5px) with -0.01em leaves ~6% headroom. Net: about
    1px smaller and a great deal heavier, which is the trade the report asked
    for.

    THE HEADROOM ASSERTION IS NEW AND IS THE USEFUL PART. "Does it fit" is not
    enough here: the suite runs with no network, so Space Grotesk never loads
    and every width it measures is system-ui's. A label one pixel inside its
    box in the sandbox can still ellipsise on the phone. The new check fails
    anything using more than 97% of its box — and it proved itself
    immediately, catching the old clamp at 390px with "Ingredients 77/78",
    which FITS in the sandbox and would not have on a device.
    IT MEASURES A RANGE OVER THE TEXT, NOT scrollWidth. The button clips its
    overflow, so scrollWidth is just the box again and can never report how
    much room a label that fits actually has — the first version of this
    reported every tab as 64/64 and failed everything.

    THE WEIGHT AND THE COLOUR ARE ASSERTED, because they ARE the change: a
    later tidy-up that put the bar back to 500, or back to `faint`, would
    leave every other test in the file green.

61. [DONE] THE FIELDS ARE 16px, SO NOTHING HAS TO BAN PINCH ZOOM.
    index.html carried `maximum-scale=1`, and its stated reason was real: iOS
    Safari zooms the page whenever a focused field's computed size is under
    16px. The cure was worse than the disease, though — it blocks PINCH zoom
    on Android for everyone, permanently, which is a WCAG 1.4.4 failure and
    takes away the one tool somebody with poor eyesight has on any page.

    THE THRESHOLD IS THE FIX. 16px is not a look, it is the number Safari
    checks. Every field in the app is 16px now — inputStyle, the two store
    dropdowns, the four aisle boxes (52px wide -> 60px to hold the bigger
    digits), the recipe notes textarea, and the three monospace backup/export
    boxes — so there is nothing left to suppress and the ban could go.
    THE ALTERNATIVES WERE CONSIDERED AND ARE WORSE: `font-size: 16px` on
    :focus only works, but the text jumps size under the cursor mid-typing; a
    transform: scale() wrapper breaks tap targets. Sizing the fields properly
    is also just better — 16px is easier to read and to hit.
    THE COST, PLAINLY: the export and backup textareas went from 11-12px
    monospace to 16px, so a backup blob is a good deal bigger on screen. It is
    a thing you copy, not a thing you read, and an exception would make the
    rule ("no field under 16") one nobody can check.

    BOTH HALVES ARE ASSERTED, because either alone rots back into the old
    behaviour: one 14px field put back brings the zoom-on-focus with it, and
    the next person to notice will "fix" it by restoring the ban.

    IT BROKE A LAYOUT, AND 164 BROWSER TESTS DID NOT NOTICE. The bigger "Add a
    store" field pushed its button 6px off a 320px screen. listrow, tabbar and
    reach each check the one row they are about, and between them they missed
    it — the cause was a missing `minWidth: 0` on one flex input, which the
    identical row eight lines below already had.
    fits.spec.mjs is the answer: every tab, at 320 and 390, WITH THE PANELS
    OPENED, nothing off the side and no sideways page scroll. Half the app's
    fields live behind a disclosure, so a sweep of the collapsed screens would
    have missed exactly this. A rule that holds everywhere needs one test that
    looks everywhere, or it is only ever as good as the last place somebody
    remembered to check.

62. [DONE] TWO REPORTS FROM REAL USE: the search box blended in, and a week
    showing only Dinner needed a way to add a meal.

    A. SEARCHFIELD, ONE DEFINITION FOR THREE COPIES. Meals, Ingredients and the
    meal picker each carried the same twenty lines, already drifting — the
    clear button sat at right:4 in two of them and right:20 in the third, and
    the same field was labelled "Search meals" in one place and "Search meals
    or ingredients" in another. It is also the control both long tabs are
    unusable without: 12 screens of ingredients, 6.8 of meals.
    IT MAKES A STATEMENT NOW, which is what was asked for. On a paper-coloured
    page a hairline border round a white box reads as decoration. It carries
    the app's own "this is interactive" language — the soft green fill and
    green rule the selected tab already uses — plus a magnifier, the one glyph
    nobody has to learn.
    THE PLACEHOLDER COLOUR IS NOW NAMED. The browser default is around #757575,
    which is 4.0:1 on that fill, under the 4.5 floor item 51b just fixed
    everywhere else. `::placeholder` in App's style block, since a placeholder
    cannot be styled inline. C.faint gives 4.72.

    B. [SUPERSEDED BY ITEM 63 — the "+" was the wrong answer.] THE "+" IS PER
    DAY, AND THAT IS THE WHOLE POINT. Item 59 collapsed the
    week to the meal types in use and put the rest behind one button at the top
    reading "Add breakfast, lunch, dessert" — wordy, easy to miss, and GLOBAL:
    it put three empty rows on all seven days so you could fill one, which is
    most of the emptiness the collapse had just removed. "Add a meal" is a
    thing you do to Tuesday.
    Each day's heading now carries a 44px "+" (item 51a's rule — it is the
    control the tab exists for), revealing that day's unused slots and nothing
    else. Mutation-tested: the version that reveals every day fails with "only
    Tuesday should have gained a Lunch row, found 7".
    IT FOLLOWS slotsEditable like every other slot control, so on a week you
    are already shopping for it sits behind Edit. A stray "+" cannot start
    rearranging a plan you are buying against.
    Still nothing stored: planning the meal is what makes its type stay.

    The e2e helpers take the same step a person does — the day's "+", not a
    global reveal. A test that routes around a new control stops describing the
    app.

63. [DONE] THE WEEK PLAN: WHAT IS ON THE DAY, PLUS ONE "CHOOSE A MEAL".
    Third shape for item 51d, and the one that was right. Worth keeping the
    other two, because the mistake is easy to repeat:
      - 4 types x 7 days, always. 28 slots to hold 4-7 meals; 2.5 screens to
        read four dinners.
      - types-in-use + a "+" to reveal the rest (items 59, 62B). Compact, but
        it hid something behind a control you had to find first, and the
        report on it was "no plus sign, that is hard to see". A glyph you have
        to discover is not more discoverable than a row that is simply there.
      - NOW: a day shows the meals ON it, then one "Choose a meal" row — the
        same button an empty slot always had. Nothing hidden, nothing to
        reveal, no new control to learn.

    THE MEAL TYPE IS CHOSEN WHEN THE MEAL IS. That is the piece that makes it
    work: a day no longer needs a row per type to let you say "this is a
    lunch", because the picker asks. The Seg offers only the types still FREE
    on that day plus the one selected — a day holds one meal per type, so an
    option that would silently replace an existing meal is not an option.
    DEFAULTS TO DINNER, not to the first free type in MEAL_TYPES order, which
    is BREAKFAST. This app exists to shop for dinners — that is where the
    "4-7 of 28" measurement came from — so the common case is one tap on the
    meal and nothing else. journey.spec caught the wrong default by reading
    the plan back; on screen the two are identical.
    !isGuest, NOT slotsEditable, on the "Choose a meal" row. An EMPTY slot has
    always been fillable in either mode — you do not press Edit to plan into a
    day with nothing on it — and gating the new row on slotsEditable quietly
    took that away. Three specs caught it.

    planTypesInUse is GONE, with its tests: the design that needed it is the
    one that was replaced. So is the "a type in use anywhere shows everywhere"
    rule — the rows are what is actually on the day now, so a Sunday breakfast
    appears on Sunday and nowhere else.

    page.planMeal MOVED INTO THE HARNESS. Five specs had their own copy of
    "plan a meal onto a day", and all five broke together the moment the Week
    tab stopped carrying a row per meal type. One definition of a flow the
    whole suite depends on.

64. [DONE] "SEARCH MEALS OR INGRE". Reported from the phone: the Meals tab's
    search placeholder was cut off. Two causes, both fixed.

    THE PINNED ROW CARRIED THREE CONTROLS — search, Filter and the primary Add
    — where the Ingredients tab has always carried two, with "add" in its own
    band above. Add moved above and became "Add a meal". Adding a meal is
    occasional; finding one is what you do while scrolling 6.8 screens.
    NOT INSIDE THE StickyBar, for the reason the Ingredients tab already
    records: two pinned bands eat the screen.

    THE PLACEHOLDERS WERE STILL TOO LONG, and the numbers say why: a search
    field is a phone's width minus a Filter button, and the magnifier and clear
    button take 62px of THAT. "Search meals or ingredients" needs 225px against
    149 at 320px — it did not fit even after Add moved (219 at 390px).
    They are one word now, "Search", and the SCOPE lives in the aria-label,
    where it costs no width and is what a screen reader actually wants. On
    screen the magnifier and the tab you are on already say it.
    THE SAME MEASUREMENT CAUGHT THREE MORE that had been cut off all along and
    nobody had noticed: "Add shopping item (e.g. paper towels)" needed 307px in
    a 179px field, "Add an item (e.g. coffee, paper towels)" 313 in 113. The
    "(e.g. …)" hints were never visible on a phone. Short names now, with the
    full description in the aria-label.

    A PLACEHOLDER THAT OVERFLOWS IS INVISIBLE TO EVERY OTHER TEST. It does not
    widen its box, scroll the page, or overlap anything — it is simply cut, so
    the fits.spec.mjs sweep could not see it and neither could the eye at a
    glance. It is measured directly now: the placeholder rendered in the
    FIELD'S OWN FONT, against the room inside its padding, on every field in
    the app at 320 and 390.

    TESTS LOCATE FIELDS BY THEIR ACCESSIBLE NAME NOW, not by placeholder.
    Shortening the copy broke 47 browser tests at once, which is the same
    lesson the harness already carries about waiting on prose: a placeholder is
    cosmetic and an aria-label is a contract.

65. [DONE] THE APP'S OWN HELP WAS LYING ABOUT RESTORE. Found while looking for
    a clean stopping point, which is when the FAQ is worth re-reading: it
    describes BEHAVIOUR, so every behaviour change is a chance for it to go
    quietly wrong, and nothing tests prose against code.
    "It also resets what is ticked off, per-item store reroutes, already-bought
    amounts, hand-added items and which staples are marked Need" — true when it
    was written, false since item 54, which carries all of that across by name.
    A user reading that would avoid a restore for a reason that no longer
    exists, or lose a trip they did not need to.
    Rewritten to say what actually happens, INCLUDING what is still lost: an
    ingredient the shipped catalog does not have, and any store or aisle set by
    hand.
    "How does something get onto the shopping list?" now mentions that the week
    asks which meal of the day it is (item 63), and starts on Dinner.

    STILL OUTSTANDING, for whoever picks this up:
      - the in-app notes that assume TWO PHONES specifically, rather than a
        household of any size. Asked for, not started.
      - item 50: whether a guest link should be single-use. Undecided, and the
        rules would have to do it, not the client.

66. [DONE] A PASS OVER THE PROSE — displayed copy, code comments, and this
    file. Nothing tests prose against code, so a comment is only as true as
    the last person who read it next to what it describes.

    COMMENTS THAT DESCRIBED A DESIGN THAT NO LONGER EXISTS:
      - WeekTab still explained the "+" that reveals a day's other meal types
        (items 59/62B), which item 63 replaced two changes ago, and named
        `shownTypes`, a variable that is gone.
      - ui.jsx's BackToTop said the sticky bar is "search + one Filter button
        + the primary action". Add moved out of it in item 64; the comment
        was the reason someone would put it back.
      - ListTab twice called the store control "the row's store dropdown". It
        has been in the item's PANEL since item 104, and it asks rather than
        having two fields.
      - The UI-density note ended "Still open: the List row", done in 104.
      - Item 59's shape was superseded by 63 and item 62B's "+" by the same;
        both now say so where a reader lands, rather than leaving three live
        descriptions of one tab.

    DISPLAYED COPY THAT ASSUMED EXACTLY TWO PHONES. Asked for a while back.
    The app has been a household of any size since item 37 — a guest is a
    third participant by design — but the words had not caught up:
      first-run screen   "shared between two phones" -> "shared live with
                         whoever you shop with"
      How it works       "Both phones see the same {List}" -> "Everyone in
                         the household"
      the store question "on both phones" -> "for everyone in the household"
      preferences        "so both phones agree" -> "so everyone sees the same
                         thing"
      restore warning    "discarded, on both phones" -> "for everyone in the
                         household"
      invite help        "on the other phone" -> "on the joining device"
      code copied        "your other phone" -> "the other device"
      FAQ                "How do I add the other phone?" -> "...another phone
                         or another person?"
    ARCHITECTURE COMMENTS KEEP "TWO PHONES" and that is deliberate: in
    lib.js, sync.js and main.jsx it names the real concurrency case the
    narrow-write machinery exists for. It is a concrete example, not a claim
    about who may use the app.

    ITEM 51 WAS MISSING FROM THIS FILE, and I deleted it. #105 landed the UX
    evaluation in main while my branch was cut from an older main; restarting
    the branch with `git diff origin/main HEAD` then carried a deletion of it
    as part of "my" changes. Nine references to 51a-51e were dangling.
    Restored from main, with a line saying which parts are done.
    THE LESSON FOR THE NEXT RESTART: `git diff origin/main HEAD` is not "my
    work" once main has moved — it is my work PLUS the undoing of everybody
    else's. Prefer format-patch of the specific commits, and diff the result
    against main before pushing.
